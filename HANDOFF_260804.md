# 인계 — 2026 세제개편안 계산기 + 세무법인 재오픈 (260804)

> 저장소 `D:\jt-data\jttax-cta` · **main push 완료 (260805, `86c0296`)**
> 🟢 **www.jttax.co.kr 라이브** · **Codex 교차검토 R1~R14 수행 완료**(수렴 미달 상태에서 오너 지시로 종료)

---

## 배경

- **핫이슈**: 재경부 「2026년 세제개편안」 2026.8.3 발표. 부동산 세제 대개편(양도세 장특공제 거주 전환·종부세 가액 기준 일원화). 검색 수요 폭발 시점.
- **세무법인 설립 완료** → 종전 "세무법인 표기 = 세무사법 위반" 금지선 해제. www.jttax.co.kr 재폐쇄 사유(`fb4f91a` "세무법인 설립 전 오픈 보류")가 소멸 → revert로 사이트 전체 복원.
- 오너 결정: 명칭 **"제이티 세무법인"** / 계산기는 **www 통합**(calc 서브도메인 아님) / 사업자번호 **자리 비움**.

## 완료

| # | 항목 | 상태 |
|---|---|---|
| 1 | 사이트 복원 (계산기 12 + 인사이트 29 + desk 2) | ✅ revert `c4582e8` |
| 2 | 브랜딩 "세무회계"→"세무법인" 351건 + "JT TAX"→"JT TAX CORP." 54건 | ✅ 잔존 0건 검증 |
| 3 | 개인 사업자번호 699-10-01720 제거 44곳 + 렌더 가드 | ✅ 잔존 0건 |
| 4 | JSON-LD postalCode 06242 | ✅ |
| 5 | **개편안 양도세 계산기** (4개 연도 비교) | ✅ E2E 통과 |
| 6 | **개편안 종부세 계산기** (3개 연도 비교) | ✅ E2E 통과 |
| 7 | SEO 랜딩 2p (실검색어 기반 title·H1·FAQ) | ✅ sitemap 44 URL |
| 8 | 허브 최상단 핫이슈 섹션 + 결과 하단 타 계산기 7종 크로스링크 | ✅ |
| 9 | **가상자산(코인) 세금 계산기** (260805 추가) — 2027.1.1 시행 예정 | ✅ E2E 통과 |
| 10 | og-image 구 상호 픽셀 각인 제거 + 생성 스크립트화 | ✅ 육안 확인 |
| 11 | 「국세청 OB」 문구 삭제 (오너 확인 — 경력자 없음) | ✅ 잔존 0건 |
| 12 | 인사이트 「2026 세제개편안 총정리」 + 마크다운 표 렌더링 지원 | ✅ |
| 13 | 부동산 계산기 3종(재산·종부·취득) 최신본 이식 | ⚠️ 엔진 미배포로 «무효» — 아래 참조 |

**핵심 파일**: `project/src/ReportReform2026.jsx`(신규, 개편안 수치 SSOT 포함) · `calculators/2026-tax-reform-{capital-gains,property-tax}.html`(수기, 빌드가 안 덮음) · `project/calculators/calculators.data.mjs`(`custom:true` 2건)

## 검증 증거

- **Node 단위검산 27건 전건 통과** — `C:\Users\jjunt\AppData\Local\Temp\claude\...\scratchpad\rf_test.js`
  (실제 .jsx의 계산부를 잘라내 그대로 실행 → 수기 계산과 대조. **재실행 시 이 파일을 프로젝트로 옮길 것 — 스크래치패드는 세션 종료 시 소멸**)
- **Node 단위검산 18건 전건 통과** — `project/tests_cr_crypto2027.js` (가상자산 계산기)
- **브라우저 E2E**: 양도세 20억/취득 10억/보유12·거주12 → 2026년 1,412만원 / 2027~ 818만원 (Node 검산치와 일치). 종부세 공시 18억·66세·거주12년 → 69만원 → 50만원. 코인 5천만 매도·1천만 취득·2026말 4천만 → 1,595,000원. 콘솔 에러 0.
- **PDF 원문 4종 교차검증**(개조식·상세본·문답·인포그래픽) 완료 — 종부세 세율표 병합셀은 x좌표 실측 + 이미지 렌더 + 비병합 원표로 3중 확정.

### 검증 중 적출한 결함 3건 (전부 수정 완료)
1. **장기거주 1주택 기본공제 2,500만원 시행 = '28 → '27** — 장특공제 이원화만 1년 유예이고 이 항목은 유예 없음. 제 최초 구현이 틀렸고 PDF 교차검증이 잡아냄.
2. **부동소수점 절단으로 세액 10원 과소** (`0.42+0.15 = 0.5700000000000001` → 산출세액 …999.99994 → 10원 절사가 10원 더 깎음). `Math.round` 선행으로 수정.
3. **`JT_KNOWN_SUBS` 화이트리스트 누락** → 신규 딥링크가 **에러 없이 조용히** 허브로 리다이렉트. 재발 방지 주석 삽입 — **새 계산기 추가 시 반드시 이 배열에 키 추가**.

---

## 내일 이어서 할 일

### 🔴 배포 전 필수
1. ~~og-image 재제작~~ ✅ **해소(260805)** — `project/assets/build-og-image.py`로 재생성, 렌더 육안 확인. 다음부터는 이 스크립트만 고쳐 재실행할 것(손으로 만들지 말 것).
2. ~~「국세청 OB」 문구~~ ✅ **해소(260805)** — 오너 확인 결과 법인에 국세청 근무 경력자 없음 → 삭제하고 사실 서술로 교체. 연고 표방·최상급 표현 전수 재확인 0건. (`Data.jsx:228` 「삼성세무서 납세보호위원회 위원」은 실제 이력이라 존치)

3. ✅ **Codex 교차검토 R1~R14 완료 (260805)** — 한도 해제 후 14라운드 수행.
   결과 파일: `project/codex-review-260805-r{1..14}.md`, 프롬프트: `codex-review-260805.prompt.txt`

   **잡은 결함: P0 2건 · P1 21건 · P2 9건.** 세액이 틀리던 것만 8건.

   | R | 신규 P0/P1 | 대표 결함 |
   |---|---|---|
   | R1 | P0 1·P1 4 | `1.5`년이 `15`년으로 읽혀 비과세(0원) — 실제 7.24억 |
   | R2 | P1 3 | 가상자산 손실 이중 차감 66만원 과소 |
   | R3 | P1 1 | 의제취득가액 합계 계산 → 2,178만원 과다 |
   | R4~R9 | P1 8 | **전부 주소조회 비동기 경계** (늦은 응답·목록 소유권·stale closure) |
   | R10 | P1 1 | 중과 완화에 「보유 2년 이상」 요건 누락 → 1억 855만원 과소 |
   | R11 | **0** | 요약 표기 반올림(P2)만 |
   | R12 | P1 3 | slug 경로탈출(홈 덮어쓰기)·GA4 이중 발화·키보드 접근 불가 |
   | R13 | P1 2 | 남은 키보드 접근성·개인정보 동의문 법정고지 미달 |
   | R14 | **P0 1** | **내가 R13에서 만든 중복 attribute → 라이브 홈 백지** |
   | R15 | P1 2 | 재발방지 스모크 자체가 거짓 통과를 낼 수 있었다(수제 파서) |

   **⚠️ 수렴 조건(3라운드 연속 0건)은 채우지 못했다.** R15까지 계속 나왔고
   오너 지시로 종료했다. 이어서 하려면 R16 부터 같은 방식으로 돌리면 된다.

   **내가 1차 소스로 잡은 것(Codex 도 못 본 것)**: 비과세가 배제되면 장특공제도
   표1(최대 30%)이어야 한다 — §95② 단서상 표2(최대 80%)는 「대통령령으로 정하는
   1세대1주택」 전용. Codex 가 제시한 470,728,500원은 장특공제를 0으로 본 값이고
   **416,691,000원이 맞다**(R2 에서 Codex 도 인정).

   **내가 기각한 Codex 지적 1건**: 「2027년 6~12억이 SSOT 에선 1.0%」는 오독.
   코드의 2027 `heavy` 구간은 실제로 `0.013`(1.3%)이다.

### ✅ 해소됨 — 엔진 배포 완료 (260805). 아래는 이력이다

> **이 절의 "미배포" 서술은 260805 배포로 해소됐다. 낡은 근거로 읽지 말 것.**
> 실측 재확인: `{address:'정릉로 305', dong:'102', ho:'601'}` → `411,000,000`(match_quality=exact) /
> `{dong:'999', ho:'9999'}` → 「이 단지에서 찾지 못했습니다(총 860세대)」 / 도곡동 467 → 1,290세대 되묻기.
> 프론트도 동·호를 «별도 필드»로 넘기도록 고쳤다(커밋 `0597496`) — 종전엔 개편안 계산기만
> 주소 한 줄로 호출해, 사용자가 동·호를 정확히 써도 늘 되물었다.

**~~공시가 자동조회의 「동·호 되묻기」가 라이브 엔진에 없습니다.~~** 260720에 코드는 작성됐는데 fly.io 배포가 안 됐습니다.

- 근거: `jt-tax-engine.fly.dev/v1/lookup/price` 직접 호출 결과 **dong/ho 포함 요청과 미포함 요청의 응답이 완전히 동일**하고, 다세대 단지 주소(도곡동 467)에 `needs_unit_selection` 없이 `manual_input_required:false` + 단일 금액(60.68억)을 반환.
- 엔진 소스에는 있음: `jt-taxlab/services/tax-engine/app/routers/lookup.py:94,367` + 테스트 `tests/test_lookup_unit_selection_260720.py`
- **프론트 3종은 이식해 뒀지만 엔진이 그 플래그를 안 주므로 동작상 구버전과 같습니다** — 즉 이식만으로는 70억 오차가 안 고쳐집니다.
- **필요 조치**: `cd jt-taxlab/services/tax-engine && flyctl deploy` (프로덕션 배포 = 오너 승인 사항이라 실행하지 않음). 배포 후 위 curl로 `needs_unit_selection` 반환을 재확인할 것.

### 🔴 260805 사고 — 라이브 홈이 20분간 백지였다

R13 에서 접근성을 «정규식 일괄 치환»으로 넣다가, 이미 `role="button"` 을 갖고
있던 앵커에 `role="link"` 를 덧붙여 **중복 attribute** 를 만들었다. Babel 이
`Home.jsx` 를 통째로 거부해 홈이 헤더만 남았다(body 733자).

**더 나쁜 건 검증이 거짓 통과를 냈다는 것**이다. 중복을 찾을 때 쓴 정규식
`<a\s[^>]*>` 이 `onClick={() => …}` 의 `>` 에서 잘려 「중복 0건」을 반환했고,
그걸 믿고 배포했다. 계산 검산 68건은 계산부만 잘라 쓰므로 못 잡는다.

→ `project/tests_jsx_smoke.js` 신설. 중괄호 깊이로 진짜 태그 끝을 찾고,
  주석은 제외하며, 중복 attribute·키보드 접근 불가를 검출한다.
  **JSX 를 손대면 이걸 먼저 돌릴 것.**

### 배포 전 게이트 4종 (전부 통과 상태)

```bash
npm install     # 최초 1회 — @babel/parser (스모크가 이것 없으면 «실패» 처리)
npm run gate    # 테스트 3종 + 빌드 2종 일괄
```
개별 실행:
```bash
node project/tests_jsx_smoke.js        # JSX 실제 AST 파싱 · 중복속성 · 키보드 접근
node project/tests_rf_reform2026.js    # 양도세·종부세 44건
node project/tests_cr_crypto2027.js    # 가상자산 24건
node project/insights/build-insights.mjs && node project/calculators/build-calculators.mjs
```
※ 스모크는 **@babel/parser 로 실제 AST 를 만든다.** 손수 만든 태그 파서는
  문자열 안의 `>` 에 속아 거짓 통과를 내므로 R15 에서 폐기했다.

### 다음 단계 (계획 Step 3~5)
5. ~~인사이트 1편~~ ✅ **완료(260805)** — `insights/tax-reform-2026-real-estate.html`. 계산기 2종과 양방향 내부링크. ※ jttax-cta 인사이트 30편은 **noindex 아님**(전부 색인 허용) — 종전 메모의 「29편 noindex」는 jt-calc 저장소 얘기였음.
6. **검색엔진 등록**: 구글 서치콘솔(sitemap 제출+URL 색인요청) / 네이버 서치어드바이저(⚠️ naver.com 환경 차단 → 오너 Chrome) / 다음 register.search.daum.net
7. **지도 주소·명칭 변경**: 네이버 스마트플레이스 + 카카오맵 → "제이티 세무법인" / 강남대로78길 22, 5층 (06242). **카카오맵 구주소("강남대로 310 유니온센터 1312호") 미갱신 = 광고 유입 누수 중**
8. **법인 사업자등록번호** 발급되면 `project/src/Data.jsx`의 `businessNumber`에 넣기 (가드 이미 있어 값만 채우면 표시됨)

### 가상자산 계산기 — 알아 둘 것 (260805 추가분)
- 시행일 근거는 **소득세법 부칙(법률 17757) §5**이고, 조문은 이미 법에 들어가 있으며 **적용 시기만 부칙이 정한다**. 2022→2023→2025→2027로 세 번 미뤄졌으므로 **또 미뤄질 수 있다** — 화면·랜딩 양쪽에 그 사실을 고지해 뒀다.
- **§37⑥(2024.12.31 신설)은 계산에 반영하지 않았다** — 취득가액 확인 곤란 시 총양도가액의 50% 이하 시행령 비율을 필요경비로 인정하는 규정인데, **비율을 정할 시행령이 아직 없다**. 시행령 제정 시 SSOT(`window.JT_CRYPTO_2027`)에 추가할 것.
- **250만원은 «건별»이 아니라 «해당 과세기간» 기준**이다(§84 3호 원문). 실무 오해가 잦은 지점이라 문항·결과·FAQ에 명시했다.
- 자동완성 상위에 "코인 세금 안 내는 방법"·"피하는 법"이 있으나 **그 프레임은 쓰지 않았다**(탈세 조언 금지). 후속 콘텐츠에서도 같은 선을 지킬 것.
- 미반영: 대여(스테이킹·렌딩) 소득, 해외 거래소·개인지갑, 채굴·에어드랍·하드포크, NFT 해당 여부, 여러 번 나눠 산 경우의 취득가액 산정 순서(시행령 위임).

### 알아 둘 것
- `D:\jt-data\jt-calc`(별도 저장소)의 부동산 6종은 **이쪽보다 최신**(공시가 동·호 되묻기 수정 = 70억 오차 결함 대응). 이번엔 이식하지 않음 — www의 6종은 아직 구버전. **별도 라운드 필요.**
- `calculators/*.html`·`insights/*.html`은 **빌드 산출물**(`project/{calculators,insights}/build-*.mjs`). 직접 고치면 다음 빌드에 날아감 — 데이터 소스를 고칠 것. 단 개편안 2p는 `custom:true`라 보존됨.
- 개편안은 **정부안**. 국회 통과 시 수치 변동 가능 → `ReportReform2026.jsx`의 `window.JT_REFORM_2026` 블록만 고치면 전 화면 반영됨.

---

## ⏸ 미완 — 개별주택 2025년 합본 적재 (260805 중단, 이어서 하면 됨)

**왜 하는가**: 엔진의 개별주택 DB에 **2026년치만** 실려 있어, 2026년 공시 «대상이 아닌»
필지(멸실·용도변경 등)가 통째로 조회 실패였다. 실측 — 종로 가회동 11-42는
2026년엔 없고 2025년에 5.24억이 있다. **신규 커버 65,840 필지(+1.7%)**.

**왜 도쿄 IP 얘기가 나오나**: vworld 는 해외 IP 차단이라 fly(도쿄)에서 실시간 조회가 막힌다.
그래서 로컬 SQLite 가 1순위다. ⛔ **fly 에 한국 리전은 없다** — 실측 확인:
아시아·태평양 = 뭄바이(bom)·싱가포르(sin)·시드니(syd)·도쿄(nrt) 뿐. 리전 이전은 «불가».

### 이미 끝난 것
- `D:/jt-data/공시가격벌크/house_price_2025.sqlite` (386만행, 319MB) — 원본 ZIP `20250810` 로 생성
- `D:/jt-data/공시가격벌크/house_price.sqlite` (**합본 771만행, 693MB**, integrity ok)
- **동작 검증 완료(코드 변경 0)**: `query_house` 가 `year=(SELECT MAX(year) ... WHERE pnu=?)`
  로 «집집마다 최신 연도»를 보므로, 2026 있는 집은 2026 유지 / 없는 집만 2025 로 떨어진다.
    가회동 11-42 → 2025년 524,000,000  ·  성북 62-18 → 2026년 469,000,000
- `jt-taxlab/services/tax-engine/fly.toml` 의 `HOUSE_PRICE_DB` 를
  `/data/house_price.sqlite` 로 **로컬 편집만 해 둠 (배포 안 함)**

### 남은 것 — 업로드가 두 번 끊겼다
```
1회차: 693MB 통짜 → 39MB 에서 중단(느림, 90분 예상)
2회차: 319MB(2025분만) → "connection lost (72,318,976 bytes written)"
```
⚠️ **`flyctl ssh sftp put` 은 대용량에서 끊긴다. exit code 0 이어도 실패한다** —
반드시 `ls -la /data` 로 **크기를 눈으로 대조**할 것.
⚠️ Git Bash 에서는 `MSYS_NO_PATHCONV=1` 필수. 안 하면 원격 `/data/...` 가
`C:/Program Files/Git/data/...` 로 바뀌어 조용히 실패한다(1회차에 실제로 당함).

**권장 경로 — 서버에서 만들기**(업로드 총량 절반, 재개 가능):
1. `/data/house_price_2025.sqlite` 업로드 (319MB). 끊기면 이어서 재시도 후 크기 대조
2. 서버에서 병합 — 2026 원본은 남겨 둔다:
   `cp /data/house_price_2026.sqlite /data/house_price.sqlite`
   `python3 -c "import sqlite3;c=sqlite3.connect('/data/house_price.sqlite');c.execute(\"ATTACH '/data/house_price_2025.sqlite' AS y\");c.execute('INSERT INTO house_price SELECT * FROM y.house_price');c.commit()"`
3. `PRAGMA integrity_check` + 행수 7,717,823 확인 → `rm /data/house_price_2025.sqlite`
4. `flyctl deploy` (fly.toml 변경 반영) → 라이브 재확인

**정리 필요**: 볼륨에 2회차 부분 파일 `/data/house_price_2025.sqlite` (72MB) 가 남아 있다.
**라이브 엔진은 무손상** — 배포를 안 했으므로 여전히 `house_price_2026.sqlite` 를 본다.

## 🔎 별건 — 건축물대장 래퍼가 «데이터 없음»으로 위장 중 (260805 발견, 미수정)

`jt_realestate/building_info.py` 가 **XML 만 파싱**하는데 data.go.kr 이 응답 기본값을
**JSON 으로 바꿨다.** 파싱 실패가 빈 리스트가 되어 "해당 조건의 건축물대장 정보가 없습니다"
로 나간다 — 강남파이낸스센터도 «없음»이 떴다. API 는 멀쩡하다(직접 호출 실증):
```
GET .../BldRgstHubService/getBrTitleInfo?sigunguCd=11680&bjdongCd=10100&bun=0737&ji=0000
→ 200, platArea(대지면적) 13,156.7 / totArea(연면적) 212,615.29
```
고치면 **토지 자동계산(개별공시지가 × 대지면적)** 이 열린다. data.go.kr 은 IP 차단이
없어 도쿄에서도 되고, 키(`DATA_GO_KR_KEY`)도 이미 배포돼 있다. 벌크 수집 불필요 —
주소당 API 1회. 단 **건물 있는 필지만** 되고, 나대지는 토지대장 쪽 별도 API 가 필요하다.
