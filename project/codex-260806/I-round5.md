신규 발견 6건 — 모두 P1입니다.

- [P1] · `ReportAcquisition.jsx:479` / `:190` · 화면은 “취득유형별 세율”이 반영됐다고 말하지만, 상속은 조건 없이 일반 실효세율 2.96%만 적용합니다. 무주택 1가구 1주택 상속의 0.8% 특례는 코드에도 입력항목에도 없고, `:465-468`에서 별도 미반영 가능성으로 안내합니다. · 재현: 엔진 실패 → 주택 상속 → 무주택 1가구 1주택. · 수정안: “일반 취득유형별 세율”로 축소하고, “상속 무주택 1가구 1주택 0.8% 특례”를 미반영 목록에 추가.

- [P1] · `ReportInheritance.jsx:491` / `:390` · 화면은 일괄공제 5억을 반영했다고 말하지만, 배우자만 있고 자녀가 없으면 코드는 일괄공제 대신 기초공제 2억을 적용합니다. · 재현: 엔진 실패 → 배우자 있음 → 자녀 0명. · 수정안: “일괄공제 5억(배우자 외 상속인 포함 시)·배우자 단독상속 시 기초공제 2억”으로 조건을 명시.

- [P1] · `ReportProperty.jsx:633` / `:147-159` · 화면은 미반영 항목을 세부담 상한·토지 누진구간·감면으로 한정하고 “실제보다 높게” 나온다고 말하지만, 건축물 폴백은 지역자원시설세(소방분)를 전혀 계산하지 않습니다. 이 경우 실제보다 낮게 나올 수 있습니다. · 재현: 엔진 실패 → 건축물 계산. · 수정안: 지역자원시설세를 미반영 목록에 넣고, 단정 문구를 “실제 세액과 차이날 수 있습니다”로 변경.

- [P1] · `Report.jsx:97-99`, `:403`, `:482-492` · 입력값 처리 고지는 허브에서만 렌더됩니다. `#/report/gift`, `#/report/inheritance`, `#/report/acquisition`, `#/report/property` 직접 진입 화면에는 엔진 전송·AI 이용 고지가 없지만, 각 계산기는 외부 엔진과 AI를 호출합니다. · 재현: 해당 딥링크로 직접 진입해 계산 진행. · 수정안: `JTReportPage`의 모든 계산기 서브라우트에 입력값 처리 고지를 공통 렌더하거나, 입력 시작 전 간결한 고지를 배치.

- [P1] · `ReportConvert.jsx:399-401` · 화면은 “담당 세무사에게 전달”됐다고 말하지만, `200 OK`와 `{}` 또는 성공 필드가 없는 JSON도 `data.success !== false` 때문에 성공으로 처리합니다. Web3Forms의 JavaScript 예시는 `data.success`가 참일 때만 성공으로 판정합니다. · 재현: Web3Forms 응답을 `200 {}`로 모킹 후 PDF 저장. · 수정안: `sent = res.ok && data?.success === true`로 변경. [Web3Forms React 예시](https://docs.web3forms.com/how-to-guides/js-frameworks/react-js/simple-react-contact-form)

- [P1] · `ReportConvert.jsx:94-97` · 카카오톡 연결 경로도 `res.ok`만 보고 “전달했습니다”를 표시하며 본문의 `success:false`를 검사하지 않습니다. PDF 경로에 넣은 성공 판정 보완이 이 경로에는 회귀 상태입니다. · 재현: Web3Forms가 `200 {"success":false}`를 반환하도록 모킹 후 동의하고 카카오톡 연결 클릭. · 수정안: 응답 JSON을 파싱해 `res.ok && data?.success === true`일 때만 성공 문구를 표시.