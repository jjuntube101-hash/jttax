당신은 한국 세무·프론트엔드 코드의 독립 검토자입니다. 작업 디렉터리는 D:\jt-data\jttax-cta 이고,
최신 커밋 `739ab31` 을 검토하십시오.

## 직전 검토(P-earlyreturn.md)에서 지적한 것과 그 처리
- [P0] 조기 반환보다 먼저 `window.claude.complete()` 가 폴백 세액을 외부로 전송
  → 판정식을 모듈 스코프 순수 함수 `{acq,inh,gift,prop,cgt}FallbackGaps(answers, calc)` 로 추출하고,
    `runAnalysis` 가 엔진 응답 직후 · AI 프롬프트 생성 «전» 에 이 함수로 판정해 차단 시 즉시 return 하도록 변경.
    렌더도 같은 함수를 호출.
- [P1] 일시적 2주택이 법정 요건보다 넓고 housing_count 를 무조건 1로 덮어씀
  → showIf 를 `=== 2` 로 좁히고, payload 덮어쓰기도 2주택일 때만. 3주택+「예」 잔존 조합은 차단.
    문항 문면에 법정 사유(이사·학업·취업·직장이전) 명시.
- [P1] `isRegulatedArea: 'unsure'` 가 payload 에서 `no` 와 구별되지 않음 → **미조치**.
    현재는 `unsure` 를 차단 조건으로만 쓰고 payload 에는 보내지 않습니다. 이 판단이 타당한지 봐 주십시오.
- [P1] 회귀 테스트가 AI 누설을 못 잡음 → `tests_fallback_block.js` 전면 재작성.
    3층 검사(①판정 규칙 ②게이트가 AI 호출보다 앞인가 ③차단 화면 내용)로 확장.
- [P2] 면적 「84로 가정」 문구 모순 → 문구 정정.

## 이번에 검토할 것
1. **P0 가 실제로 닫혔는가.** 5개 계산기 모두에서 차단 시 도달 가능한 외부 전송 경로가 남았는지.
   `window.claude.complete` 외에 fetch·gtag·localStorage·주소 조회 API 등 다른 경로도 포함해 보십시오.
2. **판정 함수 추출이 의미를 바꾸지 않았는가.** 종전에 컴포넌트 안에서 참조하던 지역 변수
   (`acqArea`, `nonResident`, `calc.engineErr` 등)가 함수 안에서 동일하게 계산되는지 대조하십시오.
   특히 증여세는 `(calc.precise || calc.engineErr)` 조건이었습니다.
3. **차단 리포트의 형태.** `{ calc, commentary: null, quick }` 를 setReport 하는데,
   렌더가 `commentary` 를 null 로 받아도 안전한지(차단 화면 외 경로로 샐 여지).
4. **일시적 2주택 수정의 법령 정확성** 재확인. 시행령 §28의5 의 「종전 주택등 1개 보유」 요건과
   3년 기한, 그리고 지방세법 §13의2①2호 괄호의 문언.
5. 새 회귀 테스트가 «되돌림»을 실제로 잡는가. 잡지 못하는 되돌림 시나리오가 있으면 구체적으로.
6. 그 밖에 이번 변경이 만든 새 결함.

## 보고 형식
- [P0/P1/P2] `파일:라인` · 한 줄 요약
  재현: 구체적 입력과 그때 나오는 잘못된 결과
  수정안: 무엇을 어떻게

지적이 없으면 "지적 없음"이라고 명시하십시오. 확인 못 한 것은 「미확인」으로 표시하고
추측을 사실로 쓰지 마십시오. node 는 이 환경에 없으니 실행 검증은 시도하지 마십시오.
