당신은 한국 세무·프론트엔드 코드의 독립 검토자입니다. 작업 디렉터리 D:\jt-data\jttax-cta.
검토 대상은 `77d5026..HEAD` 입니다(HEAD = 95d6501).

## 직전 검토(U-round20.md)의 지적과 처리 — 전부 채택했습니다
- [P1] 지분 미입력(50% 가정)이 정밀 엔진에 POST 된 뒤에야 차단됨. 6개 계산기가 같은 구조였음
  → 판정 함수가 2층인 점을 이용해 **runAnalysis 맨 앞에서 `fn(answers, { precise: true })`**
    로 불러 ①불확정 층만 먼저 판정하고 즉시 return. 엔진 응답 직후의 기존 게이트는 그대로
    ②폴백 한계를 잡습니다. 6개 전부 게이트 위치 < 엔진 호출 위치를 확인했고,
    브라우저 fetch 계측으로 «차단 시 추가 엔진 요청 0건»을 실측했습니다.
- [P1] AST 가 변수 «이름»만 기억해 shadowing 으로 뚫림
  → 같은 이름이 두 번 이상 선언되면 그 이름을 인정하지 않습니다(스코프 해석 불가 시 보수적).
    @babel/traverse 는 이 저장소에 없습니다. 격리 주입으로 FAIL / 정상 변수형 PASS 확인.
- [신규] 게이트가 둘인 파일에서 «하나만» 무력화하면 종전 검사가 전부 통과 → **runAnalysis 안의
  모든 판정 호출이 실제 게이트에 쓰이는지** 검사 추가(판정만 하고 안 쓰는 호출 = 무력화 흔적).
- [P2] 안전한 부정형 게이트(`if (!gaps.length) {…} else { return; }`)를 거부하던 것 수정.
  부정형은 alternate 의 확정 반환을 봅니다. `if (!gaps.length) { return; }` 는 계속 거부.

## 이번에 검토할 것
1. **엔진 전 게이트가 «①층만» 거르는 게 맞는가.** `fn(answers, { precise: true })` 로 부르면
   각 판정 함수가 실제로 ①층만 반환하는지 8개 전부 대조해 주십시오.
   특히 증여세는 `if (calc.engineErr) return [...]` 가 맨 앞에 있는데, precise:true 일 때
   engineErr 가 undefined 라 통과합니다 — 의도대로인지 봐 주십시오.
2. 엔진 전 게이트가 **중복 차단**을 만들지 않는가. 같은 사유로 두 번 setReport 되거나,
   `quickReport` 상태가 어긋나거나, 사용자가 「다시 시도」를 눌렀을 때 무한 반복되지 않는가.
3. 새 「판정 호출이 전부 게이트에 쓰인다」 검사가 **정당한 코드를 거부**하지 않는가.
   예: 로그·디버그 목적의 호출, 조건부 분기 안의 호출 등.
4. shadowing 방어(같은 이름 2회 선언 시 불인정)가 **정당한 코드를 거부**하지 않는가.
   서로 다른 함수에서 각각 `const gaps = fn(...)` 를 쓰면 2회로 세어 거부됩니다 —
   이게 실제로 문제가 되는 구조인지 8개 파일에서 확인해 주십시오.
5. 8개 계산기의 «차단 경로»를 다시 한 번 전수로: 화면·AI·클립보드·Web3Forms·gtag·
   주소조회 API·localStorage·엔진 POST.
6. 그 밖에 `77d5026..HEAD` 가 만든 새 결함.

## 보고 형식
- [P0/P1/P2] `파일:라인` · 한 줄 요약
  재현: 구체적 입력과 그때 나오는 잘못된 결과
  수정안: 무엇을 어떻게

지적이 없으면 "지적 없음"이라고 명시하십시오. 확인 못 한 것은 「미확인」으로 표시하고
추측을 사실로 쓰지 마십시오. node 는 이 환경에 없으니 실행 검증은 시도하지 마십시오.
