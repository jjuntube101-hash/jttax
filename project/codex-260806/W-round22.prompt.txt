당신은 한국 세무·프론트엔드 코드의 독립 검토자입니다. 작업 디렉터리 D:\jt-data\jttax-cta.
검토 대상은 `95d6501..HEAD` 입니다(HEAD = d319c63). **수렴 확인 라운드**입니다.

## 직전 검토(V-round21.md)에서 P0/P1 은 0건이었고, P2 4건을 전부 반영했습니다
- [P2] 재시도 버튼이 입력 불확정 차단에서 무의미 → `JTFallbackBlocked` 에 `reason` 추가.
  'input' 이면 「아래를 확인해 주시면 금액을 계산해 드릴게요」 + 「← 이전으로 돌아가 채워
  주세요」를 보이고 재시도 버튼을 «감춘다». 'engine' 이면 종전 문구 유지.
  8개 계산기가 판정 함수를 precise:true 로 한 번 더 불러 사유를 스스로 판별한다.
- [P2] 엔진 전 차단이 precise:false 로 저장돼 렌더가 ②층 사유까지 붙임
  → `calc: { precise: true, preEngineBlock: true }` 로 저장. 실측 4조합에서 ①층 1건만 표시 확인.
- [P2] 엔진 전 게이트 삭제 회귀를 못 잡음 → 6개 2층 계산기에 대해 «precise:true 로 막고
  나가는 게이트가 첫 엔진 호출보다 앞인가»를 AST 로 별도 검사. 엔진 호출은 게이트와 «같은
  함수 안»의 것만 센다(엔진 래퍼 정의부 오탐 방지). 주입 검증: 엔진 전 게이트만 삭제 →
  종전 5개 검사는 PASS, 새 2개만 FAIL.
- [P2] 로그·계측 호출 오탐 → 무력화는 «if 조건식 안»에서 일어나므로 조건식 밖 호출은 제외.
  console.debug 주입으로 오탐 0 확인.

## 이번에 확인할 것 (수렴 판정용)
1. 위 4건이 실제로 닫혔는가. 특히 `preEngineBlock` 이 «정밀 계산 성공»과 혼동되어
   다른 렌더 분기(예: 「정밀 계산 (JT택스랩 엔진)」 라벨, 단계별 계산표, 공유 문구)를
   열지 않는가 — 8개 전부 조기 반환이 그보다 앞인지 확인해 주십시오.
2. `reason` 판별을 위해 렌더에서 판정 함수를 «한 번 더» 부른다. 이 추가 호출이
   부작용(중복 계산·상태 변경·성능)을 만들지 않는가. 순수 함수인지 확인해 주십시오.
3. `JTFallbackBlocked` 가 `React.Fragment` 를 쓰는데, 이 저장소는 UMD React 전역 로드다.
   Report.jsx 가 로드되는 시점에 `React` 가 반드시 있는가.
4. 새 AST 검사 2종이 정당한 구조를 거부하지 않는가 (엔진 호출을 변수에 담는 경우,
   엔진 래퍼가 async 화살표인 경우 등).
5. 마지막 전수 확인: 8개 계산기의 차단 경로에서 화면·AI·클립보드·Web3Forms·gtag·
   주소조회·localStorage·엔진 POST 중 하나라도 열려 있는가.
6. 그 밖에 새 결함.

**P0/P1 이 없으면 「수렴」이라고 명시해 주십시오.** 지적이 없으면 "지적 없음"이라고 쓰십시오.
확인 못 한 것은 「미확인」으로 표시하고 추측을 사실로 쓰지 마십시오.
node 는 이 환경에 없으니 실행 검증은 시도하지 마십시오.

## 보고 형식
- [P0/P1/P2] `파일:라인` · 한 줄 요약
  재현: 구체적 입력과 그때 나오는 잘못된 결과
  수정안: 무엇을 어떻게
