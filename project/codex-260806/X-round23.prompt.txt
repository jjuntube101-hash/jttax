당신은 한국 세무·프론트엔드 코드의 독립 검토자입니다. 작업 디렉터리 D:\jt-data\jttax-cta.
검토 대상은 `d319c63..HEAD` 입니다(HEAD = 1d3006b). **최종 확인 라운드**입니다.

## 배경
직전 검토(W-round22.md)에서 당신은 「수렴」을 명시했고 P0/P1 0건이었습니다.
그 P2 2건을 반영한 것이 이번 커밋이며, **이미 라이브에 푸시된 상태**입니다.
(https://www.jttax.co.kr — 200 응답, 새 asset 버전 반영 확인)

## 이번 커밋이 한 일
1. [P2 대응] `tests_gate_ast.js` 가 엔진 호출을 셀 때 **중첩 함수 안까지** 훑어,
   게이트 «앞»에 래퍼를 정의만 하고 «뒤»에서 실행하는 정상 구조를 잘못 FAIL 냈다.
   → `walkSkippingNestedFns` 를 도입해 중첩 함수를 순회에서 제외.
     주입 검증: `const runEngine = async (b) => callEngineBody(b);` 를 게이트 앞에 두고
     게이트 뒤에서 실행 → PASS(종전엔 FAIL).
   ⚠️ 별칭(`const runEngine = callTransferEngine;`)은 여전히 못 찾는다는 한계를
     테스트 주석과 인계문서에 명시했습니다(데이터플로 추적은 하지 않기로 결정).
2. [P2 대응] 줄끝 공백 11줄 정리 (Home.jsx·Pages1.jsx·검토기록 3종). `git diff --check` 0건.
3. [회귀 추가] `tests_fallback_block.js` 에 «과잉 차단 방지» 12건 —
   평범한 입력이 8개 계산기 어디서도 막히지 않는지 확인.
4. [문서] `HANDOFF_폴백차단.md` 를 수렴 상태 기준으로 재작성.

## 이번에 확인할 것
1. `walkSkippingNestedFns` 가 **검사를 약화**시키지 않았는가.
   중첩 함수를 건너뛰면서 «실제로 게이트를 우회하는» 구조를 놓치게 되지 않는가.
   특히 IIFE, `.then(() => callEng(...))`, `await (async () => …)()` 같은 형태.
2. 「과잉 차단 방지」 12건이 **차단을 약화**시키는 방향으로 잘못 고정된 것은 없는가.
   즉 «막아야 하는데 통과로 못 박은» 케이스가 섞여 있지 않은가.
   (각 입력이 실제로 안전한 조합인지 판정 함수 본문과 대조해 주십시오)
3. Home.jsx·Pages1.jsx 의 공백 제거가 **의미 있는 변경**을 일으키지 않았는가
   (템플릿 리터럴·JSX 텍스트 안의 공백이 화면에 영향을 주는 자리였는지).
4. 인계문서의 서술이 코드와 어긋나는 곳이 있는가 — 특히 «게이트가 두 번»,
   «precise:true 면 ①층만», «차단 8종 / 제외 3종» 이 실제 코드와 맞는지.
5. 그 밖에 `d319c63..HEAD` 가 만든 새 결함.

**P0/P1 이 없으면 「수렴」이라고 명시해 주십시오.** 지적이 없으면 "지적 없음"이라고 쓰십시오.
확인 못 한 것은 「미확인」으로 표시하고 추측을 사실로 쓰지 마십시오.
node 는 이 환경에 없으니 실행 검증은 시도하지 마십시오.

## 보고 형식
- [P0/P1/P2] `파일:라인` · 한 줄 요약
  재현: 구체적 입력과 그때 나오는 잘못된 결과
  수정안: 무엇을 어떻게
