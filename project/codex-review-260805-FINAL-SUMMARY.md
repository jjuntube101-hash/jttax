# R23~R28 3계산기 전수 재감사 — 최종 종합 (발견만, 수정 안 함)

> 실행: 2026-08-05, `gpt-5.6-terra`·effort high·read-only, 8라운드(R23~R28) × 3계산기 = 24회 Codex 호출.
> 각 라운드는 직전까지 발견된 전체 목록을 프롬프트에 명시해 중복 보고를 차단하고 새 각도로만 파고들도록 설계.
> **종료 근거**: R27(양도세·종부세 "신규 발견 없음") → R28(3건 모두 "수렴" 명시, 유일한 신규는 사전에 지목해 교차확인시킨 항목뿐).
> **주의(R028)**: 아래는 Codex의 주장이며, 일부(★표시)만 Claude가 jt-law-mcp·직접 계산·node 테스트 실행으로 재확인했다. 나머지는 다음 세션에서 고치기 전에 반드시 재현할 것.
> 원본 보고서 24개: `codex-review-260805-r2{3,4,5,6,7,8}-{cgt,cre,crypto}.md`

## Claude가 이미 재확인한 사실 (재검증 불필요)
- ★ 소득세법 시행령 §88①(총평균법)·④⑤(50% 추계필요경비) 원문 — jt-law-mcp로 직접 조회해 Codex 주장과 일치 확인.
- ★ 기존 테스트 3종(`tests_rf_reform2026.js`·`tests_cr_crypto2027.js`·`tests_rf_addr.js`)을 Claude 환경에서 직접 `node`로 실행 — 전부 통과. 즉 **기존 테스트가 이번에 찾은 결함들을 커버하지 못하고 있다는 것이 사실로 확인됨** (테스트가 잘못 짜인 게 아니라 애초에 이 케이스들을 다루지 않음).
- ★ SEO 랜딩 카피(`calculators.data.mjs`)의 수치(종부세 14억/9억, 가상자산 20%·22%·250만원)는 실제 SSOT 상수와 직접 대조해 일치 확인.

---

## 🔴 양도세 개편안 계산기 (`ReportReform2026.jsx` — CGT, 13건)

### P1 — 세액이 크게 틀림 (5건)
1. `1091-1096` 2027~2029년 비교 카드가 보유·거주기간을 증가시키지 않음 → 재현 사례 1,699만원 과다
2. `239,274,325,352,1020-1046` 보유·거주기간을 날짜 아닌 소수 연수로만 판정 → 재현 사례 4억7,073만원 vs 0원
3. `900,990` 기본값과 다른 선택지에 Tab 후 Enter 시 선택 무시(위저드 공용 결함) → 재현 사례 최대 5억6,342만원 오차
4. `918,1026` 필요경비 음수 입력 시 부호 삭제되어 양수로 계산 → 8,790만원 차이
5. `918` 소수점(`.00`) 붙여넣기 시 자릿수 밀림 → 재현 사례 16만5천원인데 1억9천만원(1,150배)

### P2 — 경계값·상태 (4건)
6. `919-920,966-970` 거주기간 `'.'` 입력이 0년으로 통과
7. `1060,1247`(Report.jsx:437 공유) 라우트 이동·새로고침 시 답변 전부 소실
8. `23,914-919` 지수표기(`1e9`)·전각숫자 파싱 실패(0원 또는 진행 불가)
9. `881-928,989-1003` 선택지 `aria-pressed` 없음, 문항 전환 시 포커스 이동 없음

### P3 — 저위험 (4건)
10. `1049-1056,955-959` 고령특례 문항 선언 순서가 showIf와 반대 → 같은 질문 두 번 노출
11. `29-39` 요약 표기(`rfEok`) 반올림으로 경계값이 억 단위 위로 올라감
12. `668-754` 조회 중 라우트 이동 시 언마운트된 컴포넌트에 setState
13. (CRE와 공유) 단계별 표시 합계와 요약카드 총액이 절사 방식 차이로 최대 9원 어긋남

---

## 🟠 종부세 개편안 계산기 (`ReportReform2026.jsx` — CRE, 10건)

### P1 — 세액이 크게 틀림 (5건)
1. `433-463` 재산세 중복분 공제(종부세법 §9③) 누락 → 81,000원 과다. **기존 테스트도 틀린 값을 정답으로 고정**(`tests_rf_reform2026.js:129`)
2. `394,435-453,1219-1244` 공동명의 1주택자가 특례 미신청이어도 자동으로 1세대1주택 세액공제 적용 → 재현 사례 1,382,400원 vs 올바른 0원
3. `387` 별거 배우자가 각자 주택 보유해도 세대 판정 없이 "1채"로 1주택 특례 적용 → 1,382,400원 vs 올바른 3,816,000원
4. `897-920,990` 기본값 선택지 Tab+Enter 위저드 버그(양도세와 공유)
5. `914,918,1223,386` 소수점 붙여넣기 자릿수 밀림 → 재현 사례 2,304,000원인데 33억5,371만원(1,455배)

### P2 — 경계값·상태 (3건)
6. `739,891-892,1229-1233,1263-1266` 거주용 주택가액 자동조회가 여러 채를 계속 합산 → 2,730,000원 과소
7. 지수표기·전각숫자 파싱 실패
8. 라우트 이동·새로고침 시 답변 소실(양도세와 공유)

### P3 — 저위험 (2건)
9. `405-406,467` 공개 함수 `window.jtRfCalcCRE`가 `residentValue` 음수·`totalValue` 0/음수를 검증 없이 통과시킴
10. `426-463` 단계별 표시와 요약카드 총액이 10원 절사 방식 차이로 최대 9원 어긋남

---

## 🟡 가상자산 계산기 (`ReportCrypto2027.jsx`, 18건)

### P0 — 법령 자체가 반영 안 됨 (2건, ★법령 원문 확인 완료)
1. `20-23,341-345` 시행령 §88④·⑤(50% 추계필요경비)가 이미 있는데 "시행령 미제정" 낡은 안내, 미반영 → 최대 2배 이상 과다(재현 사례 1,045만원 vs 2,145만원)
2. `139-190,200-210,67-90` 동일 코인 복수 취득분의 총평균법(시행령 §88①) 로직 부재 → 550만원 과다

### P1 — 세액이 크게 틀림 (5건)
3. `202-209` 취득가액 0원을 공란과 동일 취급해 계산 차단
4. `229-234,250,341-345` 가상자산 간 교환거래(시행령 §88③) 미반영·미고지 → 재현 사례 825만원 누락
5. `130,160,162,165,168` 소수점 붙여넣기 — 매도·취득·시가·수수료 **4필드 전부**에서 자릿수 밀림, 최대 100배+ 왜곡
6. `69` 과세연도 구분 없이 서로 다른 연도(2027/2028) 손익을 한 해로 통산 → 110만원 과소
7. `35,133-134,75-99` 지수표기·전각숫자 파싱(동일 근본 원인)

### P2 — 경계값·UI (6건)
8. 큰 금액(2^53 초과)에서 부동소수점 정밀도 손실
9. `195-197` 새로고침·라우트 이동 시 코인별 입력 전부 소실
10. `82,298` 동일 코인명 두 행이 결과에서 구분 불가
11. `294,316` 320px 좁은 화면에서 코인명+금액 레이아웃 겹침
12. `106,259-262` 2027년 이후 취득 코인도 "2026년까지 0원 비과세" 비교에 잘못 표시
13. `Report.jsx:384` `#/report`(허브)가 `localStorage`의 이전 계산기(crypto 등)로 잘못 복원됨

### P3 — 저위험 (4건)
14. 검산 스위트가 UI(행 추가/삭제 등) 미검증
15. `146-179` 행별 접근성 라벨 부재(스크린리더로 코인 행 구분 불가)
16. `44` 죽은 SSOT 값(`effectiveFrom`,`deemedBaseDate`,`articles.estimate/filing/addendum` 미사용)
17. `349,355` 인쇄 시 결과와 무관한 탐색 UI 함께 출력

---

## 세 계산기 공통(구조적) 결함 2종
- **금액 입력 파서** (`raw.replace(/[^0-9]/g,'')`류)가 지수표기·전각숫자·소수점 붙여넣기를 전부 잘못 처리 — CGT·CRE·crypto **3파일 모두** 같은 패턴. 한 곳만 고치고 끝나는 게 아니라 **각 파일의 파서를 전부** 손봐야 함(공용 유틸로 뽑아내는 것을 권장).
- **`RfWizard`/`RfQuestion`** 위저드의 Enter키 처리가 기본값과 다른 선택지를 무시 — CGT·CRE 공유 컴포넌트라 한 번만 고치면 둘 다 해결.

## 다음 세션(opus5) 작업 순서 제안
1. 공통 구조 결함 2종(숫자 파서, Enter키 위저드)부터 — 각각 한 번 고치면 여러 P1이 동시에 해소됨
2. 가상자산 P0 2건 — 시행령 §88①④⑤ 반영(총평균법·추계필요경비), 법령 원문은 이미 확인됨
3. 나머지 P1 순서대로(재산세공제·공동명의·세대판정·기간판정·교환거래·연도혼합·음수필요경비)
4. 수정 후 Codex 재검토(R29) — 이번 세션에서 고친 것만 diff로 넘겨 §6 규약대로 수렴까지 반복
5. 기존 테스트 fixture 중 잘못된 정답(예: `tests_rf_reform2026.js:129`)도 함께 교정
