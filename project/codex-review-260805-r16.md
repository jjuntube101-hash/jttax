검토 결과: P3 1건입니다.

- **P3 · [ReportReform2026.jsx](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:606)** · 입력 `정릉로 305(101동 601호)`  
  두 번의 슬라이스 뒤 주소가 `정릉로 305( )`가 됩니다. 이 값이 627행 `base`를 거쳐 조회 헬퍼에 전달됩니다. 인덱스 오류는 아니지만 빈 괄호가 남는 주소 훼손입니다.  
  수정안: 정규화 전에 `addr = addr.replace(/\(\s*\)/g, ' ');`를 추가해 추출 후 빈 괄호를 제거하십시오.

그 외 집중 항목은 **없음**입니다.

- `도곡동`, `상계1동·목1동·신정3동`, `1호선`, `강남대로78길 22`는 미추출이고, `101동`, `101-1동`, `A101동`, `제101동`, `601호`, `B102호`, 동만/호만 입력은 의도대로 분리됩니다.
- `retryWithUnit`도 `runWith`를 거치므로 에폭을 새로 올립니다. 응답 반영 전 `stale()` 검사도 유지되어 늦은 응답이 목록·지역 상태를 오염시키지 않습니다.
- 주소 재입력은 `ask`를 닫고, 새 조회는 기존 `dong`/`ho` 상태가 아니라 새 주소 파싱값으로 `unit`을 만듭니다. 새 `needs_unit` 응답도 동·호를 다시 초기화합니다.
- `mode === 'region'`은 642행에서 `onRegion(reg)`를 먼저 호출한 뒤에만 되묻기를 건너뜁니다. 이 분기 자체로 조정대상지역 판정이 누락되지는 않습니다.
- [build-insights.mjs](/D:/jt-data/jttax-cta/project/insights/build-insights.mjs:170)의 `[ \t]*`는 개행을 포함하지 않아 다른 줄을 삼키지 않습니다. 현재 [Data.jsx](/D:/jt-data/jttax-cta/project/src/Data.jsx:321)의 대상 주석은 독립된 들여쓰기 줄 한 곳입니다.

검증: 동일 정규식·슬라이스 순서로 위 필수 입력 13건을 재현했고 결과를 대조했습니다. `git diff --check 0597496^ 0597496`는 PASS였습니다. 이 환경에는 `node`/`npm`이 없어 `npm test`는 실행할 수 없었습니다.