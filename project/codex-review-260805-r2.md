## 1. 작업 요약

반영 중 `kind = exemptOK ? 'one' : 'multi'`와 단기보유·중과 비교는 의도대로 동작합니다. 특히 제시한 CASE 14의 **416,691,000원은 맞습니다**: 과표 987,500,000원 × 42% − 35,940,000원 = 378,810,000원, 지방소득세 37,881,000원입니다. 다만 P1 3건, P2 1건을 확인했습니다.

## 2. 변경 내역 / 결함 보고

- [P1] · [ReportReform2026.jsx:758](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:758) · `acqPrice`에 `allowZero`를 주어 취득가액 0원이 통과합니다. 안내문도 상속·증여는 “그때 평가된 금액”을 넣으라고 하므로, 0은 이 단순 모델에서 유효한 취득가액이 아닙니다.

  재현 입력: 양도가액 12억, 취득가액 `0`, 경비 0, 1주택, 취득 당시 조정대상지역, 보유 5년, 거주 0년.
  잘못된 출력: 장특 10% 후 과표 1,077,500,000원, 총세액 **460,828,500원**으로 계산·제출됩니다. 0이 “모름” 또는 상속·증여를 뜻하는 잘못된 입력이어도 막지 않습니다.
  수정안: `allowZero`를 제거하고 0 초과로 검증하십시오. 무상취득은 별도 선택지와 평가액/이월과세 입력으로 분리하십시오.

- [P1] · [ReportReform2026.jsx:577](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:577), [628](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:628) · `seq` ref 자체는 React 재렌더에도 유지되어 정상입니다. 그러나 주소 변경 시 `seq.current`를 증가시키지 않고, 조회 중에는 `busy` 때문에 새 조회도 막습니다. 따라서 구 응답이 여전히 현재 값을 덮습니다.

  재현 입력: A 주소 조회를 시작해 응답 `{ amount: 1_000_000_000 }`를 지연 → 주소를 B로 수정하고 공시가격을 직접 `900,000,000` 입력 → A 응답 도착.
  잘못된 출력: B 주소 화면의 공시가격이 **1,000,000,000원**으로 덮입니다. A 응답이 `needs_unit`이면 [595](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:595)의 `onPrice('')`가 직접 입력한 9억원까지 지웁니다.
  수정안: 주소 `onChange`에서 진행 중 요청을 abort/invalidate하고 `busy`를 해제하십시오. 자동입력 값에만 출처 표식을 두어 `needs_unit`일 때 수동 입력값은 지우지 마십시오.

- [P2] · [ReportReform2026.jsx:684](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:684) · 다중 소수점이 거절되지 않고 숫자를 조용히 변형합니다.

  재현 입력: 보유기간에 `1.2.3` 입력.
  잘못된 출력: 정규식 결과가 **`1.23`**으로 바뀌어 계산됩니다. `1.`은 1년, `.5`는 0.5년으로 안전하지만, `1.2.3.4`는 `1.2.34`가 되어 `rfNum`상 0으로 처리되는 등 동작도 일관되지 않습니다.
  수정안: 상태값을 `^\d*(?:\.\d*)?$` 하나만 허용하거나, 두 번째 점 입력 시 검증 오류를 표시하십시오. 변형이 아니라 거절이어야 합니다.

- [P1] · [ReportCrypto2027.jsx:77](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:77), [107](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:107), [109](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:109), [116](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:116) · 화면은 양도가액과 취득가액을 “그 해 전체”로 넣으라면서, 별도 `otherLoss`를 다시 차감합니다. 손실 거래의 양도가액·취득가액을 이미 합계에 포함한 정상 입력에서 손실을 이중 차감합니다.

  재현 입력: 이익 코인 매도 2,000만원/취득 1,000만원, 손실 코인 매도 100만원/취득 400만원. 안내대로 `salePrice=21,000,000`, `acqPrice=14,000,000`, `otherLoss=3,000,000`, 수수료 0.
  잘못된 출력: 엔진 소득금액 **400만원**, 세액 **330,000원**. 실제 연간 순소득은 `2,100만 − 1,400만 = 700만원`, 세액은 `(700만−250만)×22%`인 **990,000원**입니다. **660,000원 과소계산**입니다.
  수정안: `otherLoss`를 제거하고 전체 양도·취득·비용 합계만 받거나, 이익거래와 손실거래를 완전히 별도 입력받아 한 번만 합산하십시오. 의제취득가액은 2026년 말 이전 보유 자산별로 적용되므로 혼합 보유분도 한 행으로 합치면 안 됩니다. [소득세법 본문](https://www.law.go.kr/LSW/lsInfoP.do?lsiSeq=267581)

종부세 `rfCalcCRE`는 이번 범위에서 새 산식 오류를 찾지 못했습니다. 재산세 중복분 공제와 세부담상한을 제외한 것은 [1072](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:1072)에 명시되어 있으므로, 실제 고지세액이라고 표시하지 않는 한 별도 결함으로 재지적하지 않습니다.

## 3. 재발 방지

- 취득가액 0 차단, 주소 변경 뒤 지연 응답, `needs_unit` 뒤 수동값 보존, `1.2.3` 거절을 회귀 테스트에 추가하십시오.
- 가상자산에는 “전체 합계 + 다른 코인 손실”과 “자산별 손익” 두 경로의 동치 테스트를 추가하십시오.
- 다주택 조정지역·보유 0.5년 조합도 추가하십시오. 현재 구현은 장특 0%, 기본·중과세액과 70% 단기세액 중 큰 값을 선택하는 구조로 맞습니다.

## 4. 검증 결과

- `git show bd69c4f` 및 변경 전후 diff를 실제 대조했습니다.
- `git diff --check bd69c4f^ bd69c4f` 통과.
- PowerShell로 소수점 정규식과 수기 산식을 검증했습니다: `1.2.3 → 1.23`, 가상자산 재현값 `330,000원 vs 990,000원`.
- `node project/tests_rf_reform2026.js`와 `node project/tests_cr_crypto2027.js`는 이 환경에 Node 실행 파일이 없어 재실행 불가했습니다.

## 5. 미확인 사항

Node 부재로 기존 49건 테스트의 독립 재실행은 확인하지 못했습니다. 그 외 이번 지적은 소스 흐름과 수기 산식으로 재현했습니다.