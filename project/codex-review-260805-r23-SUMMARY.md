# R23 — 3계산기 전수 재감사 요약 (발견만, 수정 안 함)

> 실행: 2026-08-05 21:5x. `gpt-5.6-terra`·effort high·read-only 3건 병렬(파일별 독립 프롬프트).
> **주의(R028)**: 아래는 전부 Codex의 1차 주장이며 Claude가 재현 검증하지 않았다.
> 다음 세션(opus5)에서 수정 전 각 건을 반드시 재현 확인할 것.
> 원본 보고서: `codex-review-260805-r23-{cgt,cre,crypto}.md`

## 우선순위 순 펀치리스트

### P0 — 세액이 실제와 다르게 계산됨 (2건, 전부 가상자산)
1. **`ReportCrypto2027.jsx:20-23, 341-345`** — "시행령 미제정" 안내가 낡았다. 소득세법 시행령 §88④·⑤가 이미 실제취득가액 증빙곤란 시 **50% 추계필요경비**를 규정. 미반영 시 세액이 실제의 약 2배(10,450,000원 → 21,450,000원 오류 사례)로 나올 수 있음.
2. **`ReportCrypto2027.jsx:139-190, 200-210, 67-90`** — 동일 코인 복수 취득분의 **총평균법(시행령 §88①) 계산 로직 자체가 없음**. 미매도 잔량·취득 lot을 입력받지 않음 — 재현 예시 과다 5,500,000원.

### P1 — 세액이 유의미하게 틀림 (4건)
3. **`ReportReform2026.jsx:1091-1096`(양도세)** — 2027~2029년 비교 카드가 입력한 보유·거주기간을 그대로 재사용(연도가 늘어도 기간을 안 늘림). 재현 사례 16,989,500원 과다.
4. **`ReportReform2026.jsx:239,274,325,352,1020-1046`(양도세)** — 보유·거주기간을 날짜가 아닌 사용자 입력 소수 연수로만 판정. 날짜 기준 2년 미만인데 "2년"으로 잘못 입력되면 비과세(0원)로 잘못 표시 — 재현 사례 올바른 세액 470,728,500원인데 0원 출력.
5. **`ReportReform2026.jsx:433-463`(종부세)** — 재산세 중복분 공제(종부세법 §9③) 누락. 재현 사례(공시가 18억) 81,000원 과다 표시. **`tests_rf_reform2026.js:129`도 틀린 값을 정답으로 고정하고 있음.**
6. **`ReportCrypto2027.jsx:202-209`(가상자산)** — 실제 취득가액 "0원" 입력을 공란과 동일 취급해 계산 자체를 차단.

### P2 — 경계값·부수 로직 (4건)
7. `ReportReform2026.jsx:919-920,966-970`(양도세) — 거주기간 입력에 `'.'` 단독 문자를 0년으로 통과시킴.
8. `ReportReform2026.jsx:739,891-892,1229-1233,1263-1266`(종부세) — "거주용 주택가액" 자동조회가 단일 선택이 아니라 목록에 계속 추가·합산됨(비거주 주택을 잘못 더할 수 있음). 재현 사례 2,730,000원 과소.
9. `ReportCrypto2027.jsx:35,133-134,75-99` — 큰 금액(2^53 초과)에서 IEEE-754 부동소수점 정밀도 손실.
10. `tests_cr_crypto2027.js` — 검산 스위트가 UI(행 추가/삭제, 위 P0/P1 케이스, 총평균법, 추계경비, 큰 수)를 검증하지 않음(순수 계산부만 `eval`).

### P3 — 저위험 (2건)
11. `ReportReform2026.jsx:1049-1056,955-959`(양도세) — 고령특례 문항 선언 순서가 `showIf` 의존성과 반대라 위저드에서 같은 질문을 두 번 보게 됨.
12. `ReportReform2026.jsx:405-406,467`(종부세) — 공개 함수 `window.jtRfCalcCRE`가 `residentValue` 음수 하한을 클램프하지 않음(외부 직접 호출 시에만 노출).

## 발견 없음으로 확인된 항목 (재검토 불필요)
- 가상자산: §37⑤ 의제취득가액 코인별 적용, 수수료 필요경비 합산, 손익통산, 음수 방어, §84/§64의3 이중공제 없음, 2026년 매도 비과세 표시 — 전부 정상.
- 종부세: 공정시장가액비율 연도별(60→70→80%) 실반영, 2026 현행 세율표 일치, 다주택 공제 분모0·상한100% 클램프, 정부안 미확정 고지 — 전부 정상.

## 공통 미확인 사항 (구조적 한계, 이번 라운드로 해소 불가)
- Codex 샌드박스에 Node 실행파일이 없어 `tests_*.js` 3종 전부 **실제 실행은 못 하고 코드 판독만** 했음 — 다음 세션에서 Claude가 직접 `node` 실행해 재검증 필요.
- 2026.8.3 정부 발표 원문 PDF가 저장소에 없어, SSOT의 개편 수치 자체(세율·공제 구간 값)는 저장소 내부 정합성만 확인했고 발표자료 원문 대조는 안 됐음.

## 다음 세션(opus5) 작업 순서 제안
1. P0 2건부터 jt-law-mcp로 시행령 §88 원문 재확인 후 재현 스크립트로 값 검증
2. P1 4건 재현 확인 (특히 5번은 기존 테스트 fixture 자체가 틀렸으므로 테스트도 함께 고쳐야 함)
3. 수정 후 Codex 재검토 라운드(R24)로 수렴 확인 — §6 규약대로 라운드 상한 없이 반복
