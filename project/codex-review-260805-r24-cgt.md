## 발견 사항

- **[P2] · `project/src/Report.jsx:437`, `project/src/ReportReform2026.jsx:1060,1247` · 서브라우트 이동·브라우저 뒤로가기·새로고침 시 답변과 결과가 전부 초기화됩니다.** `JTReportReformCGT/CRE`가 조건부 렌더링으로 언마운트되고, 답변은 컴포넌트 내부 `useState`에만 있습니다.  
  재현: 양도가 20억·취득 10억·1주택·보유/거주 각 12년을 입력 후 `#/report/reform-cre`로 이동하고 뒤로가기. 잘못된 출력: 첫 질문의 빈 위저드. 올바른 출력: 기존 답변과 2026 총부담 **14,124,000원**(2027년 **8,184,000원**)을 그대로 복원.  
  수정안: 주소를 제외한 답변·결과·현재 문항을 `sessionStorage` 또는 `JTReportPage` 상위 상태에 버전 키로 저장·복원하고, 라우트 변경 시 명시적으로 초기화할 선택지를 제공.

- **[P2] · `project/src/ReportReform2026.jsx:23,914-919` · 숫자 파서가 지수표기와 전각 숫자를 금액으로 해석하지 못하고 조용히 다른 값으로 바꿉니다.**  
  재현 입력: 양도가액에 `1e9`, 취득가액 `0`, 2주택·비조정·보유 3년. 잘못된 출력: 입력 UI가 `19`로 정규화되어 총세액 **0원**. 올바른 해석값은 10억원이며, 해당 조건의 총세액은 **393,591,000원**입니다. 전각 `１２３４００００００`은 빈값으로 변환되어 진행 자체가 막힙니다.  
  수정안: 금액 입력은 전각→반각 정규화 후 쉼표·공백만 제거하고, 지수표기·부호는 명시적으로 거부하는 검증 오류를 보여야 합니다. `rfNum`도 UI 필터와 같은 단일 규칙을 사용해야 합니다.

- **[P2] · `project/src/ReportReform2026.jsx:881-928,989-1003` · 키보드 완주는 가능하나 선택 상태와 문항 전환이 보조기기에 전달되지 않습니다.** 선택지는 일반 버튼일 뿐 `aria-pressed`/radio 그룹이 없고, 숫자 입력에는 연결된 `label` 또는 `aria-labelledby`가 없습니다. 다음을 누른 뒤에도 포커스가 새 문항이 아닌 기존 다음 버튼에 남습니다.  
  재현: Tab으로 “주택 수” 선택지에 이동해 Space로 선택 후 다음. 잘못된 출력: 스크린리더가 선택 여부·새 문항을 자동 고지하지 않음. 올바른 출력: “2채, 선택됨”, 다음 문항 제목과 오류가 즉시 고지됨.  
  수정안: 선택지는 `fieldset`/`legend` + radio 또는 `aria-pressed`를 사용하고, 입력에 `id`·`label`을 연결. 문항 전환 시 제목 또는 첫 입력으로 포커스를 이동하고 오류에 `role="alert"`를 부여.

- **[P3] · `project/src/ReportReform2026.jsx:29-39` · “절대 올려서 보이지 않게”라는 요약 포맷이 경계 소수값에서는 반올림 후 억 단위로 올려 표시합니다.**  
  재현 입력: 내부 공제액 등 소수 결과 `99,999,999.6`. 잘못된 출력: `rfEok`이 `Math.round` 후 **“1억원”**. 올바른 표기: 1억원 미만임을 유지한 **“9,999.9만원”** 또는 표시 원칙에 맞춘 0.99억원.  
  수정안: 내림 정책이면 `Math.round(n)`을 제거하고 절사 기준을 원금액에 직접 적용. 정확 원 단위 병기와 요약값의 기준도 일치시킬 것.

- **[P3] · `project/src/ReportReform2026.jsx:668-754`, `project/src/Report.jsx:437` · 조회 응답 대기 중 라우트를 바꾸면 언마운트된 `RfAddrLookup`에 `setInfo`·`setBusy` 등이 실행됩니다.** 문항 이동은 epoch로 취소하지만 라우트 언마운트에는 cleanup이 없습니다.  
  재현: 조정대상지역 주소 조회를 시작하고 응답 전 브라우저 뒤로가기 또는 다른 `#/report/*` 라우트로 이동. 잘못된 출력: 구 컴포넌트의 비동기 상태 갱신 시도(React 버전에 따라 경고 또는 무시). 올바른 출력: 요청 취소 또는 cleanup 뒤 상태 갱신 없음.  
  수정안: `AbortController`와 `useEffect` cleanup, 또는 mounted ref를 도입하고 언마운트 시 epoch를 무효화.

## 변경 내역

읽기 전용 감사만 수행했습니다. 파일 변경 없음.

## 재발 방지

수정 금지 요청에 따라 테스트는 추가하지 않았습니다. 위 재현값을 회귀 테스트로 고정해야 합니다: `1e9` 거부, 전각 숫자 정규화, 라우트 왕복 복원, 억 단위 경계값, 언마운트 중 조회 취소, 키보드 선택 상태.

## 검증 결과

- `jt-law-mcp`로 소득세법 §104·§95 원문을 재조회했습니다. §104의 주택 단기보유 1년 미만 70%·1~2년 60%, 조정대상지역 2주택 +20%p·3주택 이상 +30%p와 코드의 `shortTermRates` 및 `two/three` 매핑은 일치했습니다.
- §95의 현행 장특공제 표도 코드의 1주택 보유·거주 연 4%씩 최대 80%, 일반 표 보유 연 2% 최대 30%와 일치했습니다.
- 2026~2029 SSOT는 `year` 인덱스로 실제 분기됩니다. 2026/2027 장특 구조가 같은 것은 복사 실수가 아니라 설정값상 의도된 동일값이며, 2027 기본공제·2028 대체공제/한도·2029 거주공제 전환은 각각 분기됩니다.
- XSS는 신규 발견 없음: 이 파일에는 `dangerouslySetInnerHTML`/`innerHTML`이 없고 주소는 JSON POST 본문으로 전달되며 화면 렌더링은 React 이스케이프 경로입니다.
- 기존 회귀 테스트 실행은 시도했으나 `node` 실행 파일이 환경에 없어 실행 불가였습니다 (`Get-Command node,npm,deno,bun,qjs` 결과 없음).

## 미확인 사항

None.