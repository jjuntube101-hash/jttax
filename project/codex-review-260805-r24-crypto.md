신규 발견 3건입니다. 법령은 jt-law-mcp로 소득세법 §37·시행령 §88을 재대조했습니다 [API 확인].

- [P0] · `ReportCrypto2027.jsx:229-234, 250, 341-345` 및 `calculators/crypto-tax-2027.html:80, 112-119, 160` · 가상자산 간 교환거래를 과세 대상 입력으로 안내하지도, 미반영 항목으로 고지하지도 않습니다. 시행령 §88③은 BTC→ETH 같은 교환거래의 기타소득금액 산정 가액을 정합니다. 재현: 2027년 취득가 3,000만원 BTC를 시가 7,000만원에 ETH로 교환하고, 받은 ETH를 7,000만원에 현금 매도했다고 가정합니다. 화면의 “판 금액” 안내를 따라 ETH만 `판 금액 70,000,000 / 실제 산 금액 70,000,000 / 2027년 이후 취득`으로 입력하면 출력은 소득 0원·세액 0원입니다. 올바른 값은 BTC 교환 시 소득 4,000만원, 공제 후 과표 3,750만원, 소득세 750만원+지방소득세 75만원=8,250,000원입니다. 수정안: 거래유형에 `현금 매도 / 가상자산 교환`을 추가하고 §88③의 기축가상자산·교환비율 산정을 구현하십시오. 구현 전에는 입력 상단과 미반영 목록에 “가상자산 간 교환은 과세 대상이나 이 계산기에서 계산하지 않음”을 명시해야 합니다.

- [P2] · `ReportCrypto2027.jsx:195-197`, `Report.jsx:382-397, 439` · 코인 행과 결과는 컴포넌트 `useState`뿐이며 저장되는 것은 계산기 경로(`jt_report_sub`)뿐입니다. 새로고침·다른 계산기 이동·브라우저 뒤로가기로 컴포넌트가 언마운트되면 모든 입력이 사라집니다. 재현: BTC 행에 `판 금액 50,000,000 / 실제 산 금액 10,000,000 / 시가 40,000,000 / 수수료 250,000` 입력 후 새로고침. 잘못된 출력은 빈 단일 행과 계산 결과 없음입니다. 올바른 상태는 같은 입력을 복원하여 소득 9,750,000원·총세액 1,595,000원을 다시 표시하는 것입니다. 수정안: 개인정보를 영구 저장하지 않는 `sessionStorage` 복원 또는 “이 입력은 새로고침 시 사라집니다” 고지를 입력 화면에 두십시오. 결과 화면의 “입력값은 저장하지 않습니다” 문구만으로는 계산 전 이탈을 방지하지 못합니다.

- [P2] · `ReportCrypto2027.jsx:146-179` · 키보드 탭 이동 자체는 가능하지만, 행을 구분하는 구조와 선택 상태의 접근성 정보가 없습니다. 코인명 입력은 `<label>` 없이 placeholder에만 의존하고, 모든 금액 필드가 각 행에서 같은 이름으로 반복됩니다. 또한 보유시기 선택은 두 일반 버튼의 CSS만 바뀌며 `aria-pressed`, radio group, `fieldset/legend`가 없습니다. 재현: 두 행을 만든 뒤 스크린리더로 두 번째 행의 “실제 산 금액”과 “2026년 말 이전부터 보유”를 탐색. 잘못된 출력은 어느 코인 행인지와 현재 보유시기 선택값을 프로그램적으로 알 수 없는 컨트롤입니다. 올바른 상태는 예컨대 “코인 2 ETH, 실제 산 금액”, “보유시기, 2026년 말 이전부터 보유, 선택됨”으로 읽히는 것입니다. 수정안: 행마다 `<fieldset><legend>코인 n</legend>`를 두고 코인명에 고정 label을 연결하며, 보유시기는 동일 `name`의 native radio 또는 `aria-pressed` 버튼 그룹으로 바꾸십시오.

신규 발견 없음:

- `CrRows`의 `key={i}`는 중간 행 삭제 뒤에도 모든 입력이 `value={r.*}`인 제어 컴포넌트이고 `upd(i, ...)`도 현 렌더의 배열 인덱스를 사용합니다. React 인스턴스 재사용은 발생하지만 입력값이 다른 행으로 밀려 표시되는 경로는 확인되지 않았습니다.
- 코인 수량·소수점 입력 필드는 없습니다. 따라서 별도의 수량 부동소수점 누적오차는 없습니다. 다만 수량 기반 총평균법 입력 부재는 R23 지적과 중복되어 재보고하지 않았습니다.
- 시행령 §88⑥의 “국세청장이 정하여 고시” 위임 문구는 SSOT·화면에 없습니다. 다만 law.go.kr 행정규칙 검색 0건만으로 고시 부존재를 단정할 수 없고, 화면도 “실제 시행 시 세부 계산이 달라질 수 있다”고 일반 고지하므로 이를 별도 결함으로 등급화하지 않았습니다.