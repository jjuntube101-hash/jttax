### 1. 작업 요약

신규 발견 3건: P1 1건, P2 1건, P3 1건.

### 2. 발견 내역

- [P1] [ReportCrypto2027.jsx](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:134) · `CrMoney`가 `replace(/[^0-9]/g,'')`로 지수표기와 전각 숫자를 조용히 훼손합니다.
  재현: 기존보유 행에 매도 `2,000,000,000`, 실제취득 `100,000,000`, 2026말 시가 `18e8`, 수수료 `0` 입력 → 시가가 `188`로 저장되고 총세액 `417,450,000원`. 의도한 18억 원이면 올바른 총세액은 `43,450,000원`입니다. 전각 `１，８００，０００，０００`은 빈 문자열이 되어 검증 단계에서 진행 불가입니다.
  수정안: NFKC 정규화 후 숫자를 파싱하고, 지수표기는 지원하거나 명시적 오류로 거부하십시오. 어떤 경우에도 문자를 제거해 다른 금액으로 계산하면 안 됩니다.

- [P2] [ReportCrypto2027.jsx](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:106) · 2027년 이후 취득 코인도 결과 화면의 “2026년 12월 31일까지 0원” 비교에 포함됩니다([259–262줄](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:259)).
  재현: `2027년 이후 취득`, 매도 3,000만원, 취득 2,000만원 → 2027년 세액은 `1,650,000원`인데, 화면은 존재할 수 없는 2026년 매도를 `0원 (차익 1,000만원 전액 비과세)`로 표시합니다. 올바른 출력은 `해당 없음`입니다.
  수정안: 신규취득 행이 하나라도 있으면 2026 비교를 숨기거나 “해당 없음”으로 표시하십시오. 실제 연도별 거래 입력을 지원할 목적이면 매도일을 행별로 받아 연도별로 분리 계산해야 합니다.

- [P3] [ReportCrypto2027.jsx](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:44) · SSOT의 `effectiveFrom`, `deemedBaseDate`는 파일 전체에서 선언 외 참조가 없습니다. `articles.estimate`, `filing`, `addendum`도 동일합니다. `crCalc`는 SSOT 중 `basicDeduct`, `rate`, `localRateOfTax`만 읽습니다.
  재현: 시행일·기준일 SSOT를 바꿔도 계산·화면의 하드코딩된 2026/2027 문구와 분기에는 영향이 없습니다. 올바른 동작은 SSOT 변경 시 기준일·표시·분기가 함께 변경되는 것입니다.
  수정안: 날짜·조문 문구를 SSOT에서 읽도록 통일하거나, 쓰지 않을 값은 제거하십시오.

혼합보유 C9는 신규 발견 없음입니다. 코드상 기존보유 행은 `max(취득가, 2026말 시가)`, 신규취득 행은 실제취득가를 각각 적용하며, 제시 케이스는 필요경비 6,000만원·소득 2,000만원·총세액 3,850,000원으로 일치합니다. 실제 2026년 매도와 2027년 매도를 같은 화면에 함께 입력하는 기능은 없으므로 두 연도가 합산되는 경로도 없습니다.

### 3. 재발 방지

수정은 요청 범위상 수행하지 않았습니다. 위 P1 입력값과 P2 신규취득 비교 표시를 UI 회귀 테스트로 추가해야 합니다.

### 4. 검증 결과

- UTF-8로 `Get-Content ... -Encoding utf8`로 대상 파일 및 검산 파일을 실제 확인.
- `rg`로 SSOT 참조를 전수 확인: 날짜 2개와 `estimate`·`filing`·`addendum`은 선언만 존재.
- 지수표기 재현 산출: 필터 결과 `188`, 잘못된 총세액 `417,450,000`, 올바른 총세액 `43,450,000`.
- jt-law-mcp로 소득세법 제37조 전체 및 시행령 제88조 확인: 제37조 제5항의 기존보유 자산별 의제취득가액 전제와 C9 로직은 일치함. `[CACHE_CONFIRMED]`
- `node tests_cr_crypto2027.js`는 이 환경에 Node.js 실행 파일이 없어 실행 불가(`node` 명령 미인식).

### 5. 미확인 사항

브라우저와 Node.js가 모두 없어 실제 DOM 이벤트 기반 UI 회귀 실행은 확인하지 못했습니다.