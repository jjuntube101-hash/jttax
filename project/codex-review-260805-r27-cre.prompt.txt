파일은 반드시 UTF-8로 디코딩해 읽어라 — PowerShell Get-Content/Select-String은 -Encoding utf8 필수(CP949 콘솔), rg/cat이면 그대로 쓰면 된다.

서론·계획·다짐 금지. 지금 즉시 도구로 파일을 실제로 열어 확인한 뒤 발견만 보고하라.

당신은 한국 세무·웹 도메인의 적대적 코드 리뷰어다. **2026 세제개편안 종합부동산세 계산기**(`ReportReform2026.jsx`)의 **R27 — 5라운드째 최종 확인**이다. 직전 라운드에서 "새로운 실질 결함 없음(경계값 재검증 포함)"에 가까운 결과가 나왔다. 지금까지 발견된 전체 목록(다시 보고 금지):
1~8은 이전과 동일(재산세공제누락/거주용가액다건합산/음수미클램프/지수전각파싱/Enter키위저드버그/공동명의특례자동적용/라우트이탈시소실/별거배우자세대판정없음), 추가로:
9. 단계표시-요약카드 10원 절사 불일치(최대 9원 차이)
10. 공개함수가 음수·0 공시가격을 검증 없이 0원 세액으로 반환

## 이번이 사실상 마지막 라운드다. 다음만 확인하라
- 붙여넣기·자동완성 값 파싱, 여러 탭 동시 사용 시 전역 변수 공유 위험(양도세 라운드와 동일 질문).
- 소수점 이하 원 단위가 화면에 노출되는 경로.
- `RfYearCards`가 2026/2027/2028 중 특정 연도 계산이 실패하면 그 카드가 조용히 비거나 이전 값을 재사용하는가.
- 세액공제(연령+보유/거주) 각 구간 %를 SSOT에서 다시 한번 읽어, 코드의 `Math.min(누적합, 0.8)` 류 상한 클램프가 "80% 한도"를 정확히 구현하는지 — 예를 들어 연령공제 40%+보유공제 50%처럼 **둘의 합이 80%를 넘는 조합**에서 정확히 80%로 잘리는지, 90%나 100%가 새는 경로는 없는지 숫자로 재현하라.

## 규칙
- 각 지적: [등급 P0~P3] · 파일:줄번호 · 무엇이 틀렸나 · 재현 입력값과 잘못된 출력(직접 계산한 올바른 값 포함) · 수정안.
- 정말로 새로운 게 없으면 "신규 발견 없음 — 이 파일에 대한 실질적 결함은 이미 다 나왔다고 판단한다"라고 명시적으로 선언하라. 억지로 짜내지 마라.
- 수정은 하지 마라 — 발견만 보고한다(read-only).
- 한국어로 보고하라.
