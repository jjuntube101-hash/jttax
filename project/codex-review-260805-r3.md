## 1. 작업 요약

**수렴 아님** — 세액을 크게 과다 계산하는 P1 1건이 남았습니다. R2의 `otherLoss` 이중차감 제거, 주소 응답 무효화, 소수점 입력 거절은 확인했습니다.

## 2. 결함 보고

- [P1] · [ReportCrypto2027.jsx:66](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:66) · 여러 코인의 의제취득가액을 `max(취득가액 합계, 2026말 시가 합계)`로 한 번만 계산합니다. 법·국세청 안내는 가상자산별 취득가액을 산정하고, 2026년 말 이전 보유분도 **그 가상자산별**로 `max(시가, 실제 취득가액)`을 적용합니다. [국세청 안내](https://g.nts.go.kr/nts/cm/cntnts/cntntsView.do?cntntsId=238935&mi=40370), [시행규칙 신고서 작성방법](https://www.law.go.kr/LSW/flDownload.do?bylClsCd=110202&flSeq=154125509&gubun=)

  재현 입력: 모두 2026년 말 이전 보유, 수수료 0. A 코인 `매도 1억5천/실제취득 100만/2026말 시가 1억`, B 코인 `매도 1억5천/실제취득 1억/2026말 시가 100만`입니다. 화면에는 합계로 `salePrice=300,000,000`, `acqPrice=101,000,000`, `marketAt2026=101,000,000`을 입력합니다.

  잘못된 출력: 앱은 필요경비 1억100만원, 소득 1억9,900만원, 총세액 **43,230,000원**을 냅니다. 자산별 적용의 필요경비는 `max(100만,1억)+max(1억,100만)=2억원`, 소득 1억원, 총세액은 **21,450,000원**입니다. 즉 **21,780,000원 과다**입니다.

  수정안: 합계 3필드 모델을 폐기하고 자산별 행(자산/주소, 양도가액, 실제 취득가액, 2026말 시가, 수수료)을 받아 각 행의 의제취득가액을 계산한 뒤 합산하십시오. 2026년 이전 보유와 이후 취득분이 섞이는 경우도 현 이진 `heldBefore`로는 표현할 수 없습니다.

- [P2] · [ReportReform2026.jsx:577](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:577), [605](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:605) · `autoFilled`는 “자동 입력값의 출처”가 아니라 마지막 자동 입력 **문자열**만 기억합니다. 따라서 사용자가 자동입력된 10억원을 지웠다가 똑같이 직접 다시 입력해도 ref는 남습니다.

  재현 입력: A 주소 조회로 공시가격 `1,000,000,000` 자동입력 → 사용자가 같은 `1,000,000,000`을 직접 재입력 → B 주소 조회 결과가 `needs_unit`.

  잘못된 출력: 605행이 `onPrice('')`를 호출해 사용자의 직접 입력도 빈값으로 만듭니다. 계산은 차단됩니다.

  수정안: 금액 입력의 사용자 `onChange`에서 자동입력 출처를 즉시 해제하도록 부모와 상태를 공유하십시오. 단, 질문하신 “자동값을 그대로 둔 채 다른 주소 조회”는 A 주소 값이 B 주소에 남으면 안 되므로, `needs_unit`에서 지우는 현재 동작 자체는 안전합니다.

- [P2] · [index.html:14](/D:/jt-data/jttax-cta/index.html:14), [Chrome.jsx:213](/D:/jt-data/jttax-cta/project/src/Chrome.jsx:213) · 계산기별 OG 메타는 동적으로 바꾸지만 canonical은 항상 홈 URL입니다.

  재현: `#/report/crypto` 진입 후 `link[rel="canonical"]` 값.

  잘못된 출력: 여전히 `https://www.jttax.co.kr/`입니다. 계산기 랜딩을 색인하려는 구조라면 모두 홈의 중복본으로 신호를 보냅니다.

  수정안: 검색 대상이면 해시가 아닌 서버 경로별 정적/프리렌더 랜딩과 해당 canonical을 제공하고, 검색 대상이 아니면 계산기 해시 화면은 의도적으로 `noindex` 처리하십시오.

## 3. 재발 방지

자산별 의제취득가액 혼합 사례를 회귀 테스트에 추가하고, 자동입력 출처를 값이 아닌 입력 이벤트로 검증하십시오. SEO는 각 공개 URL의 canonical을 HTML 스냅샷으로 검사하십시오.

## 4. 검증 결과

- `git show 93db711`으로 R2 변경을 대조했습니다.
- 손계산/PowerShell 재현: 앱 **43,230,000원**, 자산별 정답 **21,450,000원**, 차이 **21,780,000원**.
- 정규식 확인: `1.` 및 `.5` 허용, `1.2.3` 거절.
- `git diff --check 93db711^ 93db711`은 `project/codex-review-260805-r2.md`의 trailing whitespace 8건으로 실패했습니다.
- Node 실행 파일이 없어 기존 JS 테스트는 재실행하지 못했습니다.

## 5. 미확인 사항

`loss = 0`은 [ReportCrypto2027.jsx:62](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:62)의 죽은 코드가 맞지만, 모든 사용처가 0이므로 현재 세액 오류는 아닙니다. `income = rawIncome`으로 단순화하고 반환 `loss`도 제거하는 편이 오해를 막습니다.