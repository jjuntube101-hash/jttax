신규 발견 없음 — 수렴.

- `post`는 `heldBefore === 'no'`만 포함하므로 2026년 말 이전 보유분은 정확히 제외됩니다.
- `mixed` 조건은 계산 가능한 2027년 이후 취득분에서 추계 선택/미선택이 공존할 때 정확히 경고합니다. 다른 종목 조합은 경고만 띄우고 진행을 막지 않습니다.
- JSX IIFE는 행 목록 렌더 뒤에 단일 경고만 반환하며 키·렌더 안정성 문제나 유의미한 성능 문제를 만들지 않습니다.
- 자유입력 식별값 반려 근거는 타당합니다. 검증 가능한 표준화 없이 별도 입력칸만 추가하면 별칭 우회는 그대로 남습니다.

검증: `git diff -- project/src/ReportCrypto2027.jsx` 및 UTF-8 `Get-Content`로 실제 310–329행을 대조했습니다. `npm test`는 현재 환경에 `npm` 실행 파일이 없어 독립 재실행하지 못했습니다.