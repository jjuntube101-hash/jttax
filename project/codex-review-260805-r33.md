## 발견

- [P2] [ReportReform2026.jsx](D:\jt-data\jttax-cta\project\src\ReportReform2026.jsx:1511) · 일부 연도만 문턱 미달인 비거주 1주택에도 “공제 축소가 증가의 가장 큰 원인”을 붙입니다.  
  재현: 1주택·비거주, 공시가격 13억원, 50세, 보유 3년, 거주 0년.  
  2026년은 `(13억−12억)×60%=6,000만원`, 산출세액 30만원+농특세 6만원=36만원이고, 2027·2028년은 14억원 이하라 과세대상 아님=0원입니다. 화면은 “36만원 줄어듭니다” 뒤에 공제 12→9억원 축소가 “증가의 가장 큰 원인”이라고 표시합니다.  
  수정안: 보조 문구를 `!result.some(r => r.notTaxable)`일 때만 표시하거나, `notTaxable` 연도가 있으면 문턱 미달을 원인으로 우선 표시하십시오.

- [P3] [ReportReform2026.jsx](D:\jt-data\jttax-cta\project\src\ReportReform2026.jsx:462) · 조기 반환 객체의 `fairRatio`와 `heavy`가 서로 모순됩니다.  
  재현: 2주택·조정대상지역 아님, 공시가격 합계 8억원, 거주용 0원, 2028년. 반환값은 `notTaxable:true, heavy:false, fairRatio:0.8`입니다. 일반 계산 경로상 이 입력은 `heavy:false`이므로 light 비율 70%가 맞습니다. 과세대상이 아니므로 비율을 표시하지 않더라도, 공개 함수 반환값은 `fairRatio:0.7`이어야 일관됩니다.  
  수정안: 문턱 판정 전에 `heavy`와 적용 `ratio`를 정상 경로와 동일하게 계산해 반환하거나, 문턱 미달 시 `fairRatio:null`로 두고 호출부를 명시 처리하십시오.

원문 인용 범위 대조 결과, 1세대1주택 14억원 문턱은 거주 여부와 무관하게 적용한 구현이 맞고, “초과” 처리(`<=` 조기 반환)도 맞습니다. 2026년 `null` 처리 역시 당시 12억/9억 기본공제로 결과가 동일해 타당합니다. 인용된 p39·p40·p60 범위에서 추가 누락은 발견하지 못했습니다.

## 변경 내역

없음 — read-only 검토만 수행했습니다.

## 재발 방지

위 P2 입력과 P3 입력을 회귀 CASE로 추가해야 합니다. 현재 CASE 20은 2028년 조기 반환의 `fairRatio`/`heavy` 일관성과 혼합 연도 결론 문구를 검증하지 않습니다.

## 검증 결과

`git diff --check -- project/src/ReportReform2026.jsx project/tests_rf_reform2026.js` → exit 0.  
Node가 없어 `tests_rf_reform2026.js`는 이 환경에서 실행하지 못했습니다.

## 미확인 사항

Node 실행 결과 및 브라우저 렌더링은 이 환경에서 독립 재검증하지 못했습니다.