[P2] · `project/src/ReportReform2026.jsx:1509` · `diff === 0`이 2026년과 2028년만 비교해, 2027년 세액이 달라도 “개편안이 시행되어도 종부세가 달라지지 않습니다”라고 출력합니다.

재현 입력값: 1세대 1주택·거주, 공시가격 `18,451,612,903원`, 나이 `1`, 보유 `4년`, 거주 `0년`.

잘못된 출력: 2026년 `43,571,600원` → 2027년 `39,214,440원` → 2028년 이후 `43,571,600원`인데, 결론은 “달라지지 않습니다.”로 표시됩니다. 2027년 보유 5년 공제 10%가 적용되기 때문입니다.

수정안: `diff === 0` 분기를 `result.every(r => r.total === base.total)`일 때만 사용하고, 그렇지 않으면 “2028년 이후는 현행과 같지만 2027년에는 …”처럼 중간 연도 변동을 출력하십시오. 이 입력을 회귀 테스트로 추가해야 합니다.

`heavy`를 문턱 판정 앞에서 계산한 조기 반환 경로는 정상 경로와 동일하게 `heavy` 및 `fairRatio`를 반환합니다. Node 미설치로 테스트 파일 실행은 검증하지 못했습니다.