## 1. 작업 요약

**수렴 아님** — `cea3a32`의 코인별 의제취득가액 전환은 확인했지만, 개편안 위저드의 주소 조회가 조회 중 직접 입력을 다시 덮어써 종부세를 오산하는 P1이 남았습니다.

## 2. 결함 보고

- [P1] · [ReportReform2026.jsx:578](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:578), [615](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:615), [693](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:693) · `seq`는 주소 변경만 무효화합니다. 조회 중 사용자가 공시가격을 직접 수정해도 순번이 바뀌지 않으며, 늦게 도착한 성공 응답은 615행에서 무조건 `onPrice(r.amount)`를 호출해 수동 값을 덮어씁니다. `autoIds`에서 수동 입력 시 출처를 해제해도, 이미 시작된 조회 응답에는 적용되지 않습니다.

  재현 입력값: 종부세 개편안에서 `2채`, `조정대상지역 아님`, `거주용 주택가액 0원`. 공시가격 합계 문항에서 주소 조회를 시작하고(테스트 응답 `{ amount: 3_000_000_000 }`), 응답 전 공시가격 합계를 **1,000,000,000원으로 직접 입력**합니다. 그 뒤 늦은 조회 응답을 반환합니다.

  잘못된 출력: 수동 입력 10억원이어야 2026년 과세표준은 `(10억−9억)×60%=6,000만원`, 종부세 30만원·농특세 6만원, **합계 360,000원**입니다. 그러나 조회 응답이 30억원으로 덮으면 과세표준 `(30억−9억)×60%=12억6천만원`, 종부세 `150만+210만+600만+78만=10,380,000원`·농특세 `2,076,000원`, **12,456,000원**을 냅니다. **12,096,000원 과다**입니다.

  수정안: 문항별 입력 세대(generation)를 위저드에서 ref로 관리하고, 수동 금액 입력마다 증가시키십시오. 조회 시작 시 세대를 캡처해 응답 시점에도 일치할 때만 자동값을 반영해야 합니다. `isAuto` 불리언 캡처만으로는 늦은 응답을 막을 수 없습니다.

## 3. 재발 방지

주소 변경뿐 아니라 금액의 수동 변경·문항 전환·`showIf`로 인한 비표시에도 진행 중 조회를 무효화하는 회귀 테스트를 추가해야 합니다. `autoIds`는 출처 표시에만 쓰고, 비동기 응답 적용 여부는 별도 세대로 판정하십시오.

## 4. 검증 결과

- `git rev-parse HEAD` → `cea3a32ac4abf1d51b6ede9addbd60281a5bfdb7`
- `git show cea3a32`로 코인별 `rows`, CASE 8·9, canonical, `autoIds` 변경을 대조했습니다.
- `git diff --check cea3a32^ cea3a32` → 출력 없음, 통과.
- 손계산과 PowerShell 계산 모두 수동 10억원 **360,000원**, 늦은 조회 30억원 **12,456,000원**을 확인했습니다.
- 가상자산 `crCalc`은 UI 경로에서 부분 행을 차단합니다([203–214행](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:203)). `shielded`는 행별 `mkt−acq`로 실제취득 기준 대비 과세소득 감소분이므로 손실 종목 포함 합산도 맞고, `gainIfSoldIn2026`은 차익 표시용이며 화면의 `0원`은 세액이라 정합합니다. index key도 현재 모든 입력이 제어 컴포넌트이고 행 내부 상태가 없어 값 뒤섞임은 재현되지 않았습니다.
- Node 실행 파일이 환경에 없어 `project/tests_cr_crypto2027.js`는 재실행하지 못했습니다.

## 5. 미확인 사항

None.