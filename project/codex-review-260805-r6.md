## 1. 작업 요약

수렴 아님입니다. 신규 P0는 0건, 신규 P1은 3건입니다.
R5의 “B 자동입력 후 A 지연 응답” 순서는 막혔지만, **B 시작 후 A가 먼저 도착하는 반대 순서**는 여전히 뚫립니다.

## 2. 지적 사항

- [P1] · [ReportReform2026.jsx:583](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:583), [743](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:743) · 재마운트 뒤 B 조회를 시작했어도 세대가 오르는 시점은 B의 **응답 반영 때**뿐입니다. 따라서 A가 B보다 먼저 도착하면 A가 반영되고, 오히려 B를 세대 불일치로 버립니다.

  재현 입력값과 잘못된 출력: `1주택·거주·50세·보유 1년·거주 0년`, 공시가격에 10억을 입력합니다. A(30억) 조회 시작 → 다음 문항 이동 → 복귀 → B(10억) 조회 시작 → **A 응답 먼저** → B 응답 순서입니다. A와 B 모두 시작 세대는 1입니다. A가 30억을 입력하며 세대를 2로 올리고, B는 `genNow(2) !== gen0(1)`로 폐기됩니다. 화면은 **30억**, 표시세액은 `(30억−12억)×60%=10억8천만`, 종부세 `840만원` + 농특세 `168만원` = **10,080,000원**입니다. B의 정답 10억이면 **0원**입니다.

  수정안: 입력 세대와 별도로 위저드에 문항별 `lookupEpoch`를 두고 **조회 시작 시** 증가시키십시오. 응답은 `lookupEpoch`와 입력 세대가 모두 일치할 때만 반영해야 합니다.

- [P1] · [ReportReform2026.jsx:679](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:679), [988](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:988) · `totalValue`가 주택 수와 무관하게 항상 `priceAdd`입니다. 1주택이거나 이미 합계를 수동 입력한 경우에도 주소 조회가 기존 값을 교체하지 않고 더합니다.

  재현 입력값과 잘못된 출력: `1주택·거주·50세·보유 1년·거주 0년`, 공시가격을 직접 **10억** 입력 후 같은 집을 조회하여 **10억** 응답을 받습니다. 화면은 `10억 + 10억 = 20억`이 됩니다. 표시세액은 `(20억−12억)×60%=4억8천만`, 종부세 `150만원+126만원=276만원`, 농특세 `55만2천원`, 합계 **3,312,000원**입니다. 실제 공시가격 10억의 정답은 **0원**입니다.

  수정안: 1주택은 기존 `price`(대체) 모드를 사용하십시오. 다주택도 주소별 행을 보관해 합계를 계산하거나, 최소한 “현재 합계에 추가”를 명시적으로 선택하게 해야 합니다. 현재 구조에는 잘못 더한 한 주소만 되돌리는 방법이 없습니다.

- [P1] · [ReportReform2026.jsx:606](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:606), [680](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:680) · `priceAdd` 누적 중 `needs_unit` 응답이 오면 기존의 자동 누적 합계 전체를 빈 값으로 지웁니다. 일반 조회 실패·예외는 보존하지만, `needs_unit`만 요구사항과 반대로 소거합니다.

  재현 입력값과 잘못된 출력: 2주택에서 A 조회가 10억을 자동 입력한 뒤, B 조회가 `needs_unit`을 반환하면 `isAuto === true`라 `onPrice('')`가 실행됩니다. `priceAdd` 핸들러는 기존 10억이 아니라 입력칸을 **빈 값**으로 만듭니다. 결과는 “공시가격 합계를 넣어 주세요”로 계산 불가입니다. 기존 누적값 **10억 유지**가 맞습니다.

  수정안: `accumulate` 모드에서는 `needs_unit`에 `onPrice('')`를 절대 호출하지 마십시오. 주소별 항목 모델로 바꾼다면 해당 미확정 주소 행만 오류 상태로 두고 확정된 행의 합계는 보존하십시오.

- [P2] · [ReportCrypto2027.jsx:104](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:104), [284](/D:/jt-data/jttax-cta/project/src/ReportCrypto2027.jsx:284) · 계산 결과 객체에 `heldBefore`가 없는데 화면은 `!r.heldBefore`를 검사합니다. 따라서 모든 결과에 “2027년 이후 취득이라 의제취득가액은 적용되지 않는다”는 문구가 붙습니다.

  재현 입력값과 잘못된 출력: `2026년 말 이전 보유`, 매도 5,000만원·실제 취득 1,000만원·2026말 시가 4,000만원·수수료 25만원이면 계산상 총세액은 **1,595,000원**이며 의제취득가액이 적용됩니다. 그런데 화면은 의제 적용 설명 뒤에 **“2027년 이후에 … 의제취득가액은 적용되지 않고”**를 함께 표시합니다.

  수정안: `lines`를 기준으로 `hasPre2027`, `hasPost2026`를 계산해 혼합 보유도 정확히 표시하거나, 해당 문구를 제거하십시오.

자동입력 직후 같은 문항을 재조회하는 정상 경로는 통과합니다. 조회1 자동입력 후 세대가 1이 되고, 조회2가 1을 캡처해 응답 시 일치하므로 반영됩니다. 사용자가 잘못 더한 값을 직접 수정하면 세대가 올라 기존 진행 요청은 차단되지만, 다음 성공 조회는 다시 수정값에 더하므로 주소별 되돌리기는 되지 않습니다.

`rfCalcCRE`의 재산세 중복분 공제·세부담 상한 미반영은 결과 화면에서 명시적으로 고지되어 있어([1121](/D:/jt-data/jttax-cta/project/src/ReportReform2026.jsx:1121)) 이번 지적으로 재제기하지 않았습니다. Crypto 결과 수치 산식과 SEO 랜딩의 canonical·JSON-LD·FAQ는 별도 P1을 찾지 못했습니다.

## 3. 재발 방지

- Deferred Promise로 `A 시작 → 언마운트 → B 시작 → A 응답 → B 응답` 회귀 테스트를 추가하십시오.
- `priceAdd`에 1주택 대체, 수동 합계, `needs_unit`, 일반 실패, 잘못 더한 뒤 수동 수정 후 재조회 케이스를 추가하십시오.
- Crypto 결과 문구는 이전 보유/이후 취득/혼합 행을 각각 테스트하십시오.

## 4. 검증 결과

- `git show 0e7419e`로 R5 변경을 확인했습니다.
- `git diff --check 0e7419e^ 0e7419e`, `git show --check 0e7419e`에서 공백 오류는 없었습니다.
- `node tests_rf_reform2026.js`, `node tests_cr_crypto2027.js`는 이 환경에 `node` 명령이 없어 실행하지 못했습니다.
- 위 세액은 해당 소스의 2026년 공제·60% 비율·누진세율표를 직접 적용해 손계산했습니다.

## 5. 미확인 사항

브라우저에서 지연 응답 순서를 제어한 E2E 실행은 Node/브라우저 실행 환경 부재로 확인하지 못했습니다.