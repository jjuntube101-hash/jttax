# Codex 교차검토 — 미실행 부채 (260805)

> **상태**: 🔴 미실행. 계정 사용 한도 소진으로 **차단**.
> **리셋**: 2026-08-09 00:17 — 그 전에는 어떤 모델로도 안 된다.
> **왜 남겨 두는가**: 오너 지시로 게이트 미완 상태에서 선배포했다("일단 푸시하고
> 한도 풀리면 그때 고치자"). 이미 라이브이므로 **검토는 취소가 아니라 연기**다.

## 실행 방법 (리셋 후 그대로 복사)

```bash
cd /d/jt-data/jttax-cta
"C:/jt-data/tools/codex/codex.cmd" exec \
  --model gpt-5.6-terra --sandbox read-only \
  --cd "D:/jt-data/jttax-cta" \
  -o "D:/jt-data/jttax-cta/project/codex-review-260805-result.md" \
  - < project/codex-review-260805.prompt.txt
```

프롬프트는 **반드시 stdin**으로 넣는다. 커맨드라인 인자로 주면 `codex.cmd`
배치 래퍼가 개행 이후를 잘라먹는다 (TASK-260712-004).

수렴 규약: P0/P1이 남으면 반영 → 재검토. P2 이하만 남거나 같은 지적이
반복되면 종료. 최대 5라운드.

## 시도 이력 — 재시도로 시간 쓰지 말 것

| 회차 | 일시 | 모델 | 세션 id | 결과 |
|---|---|---|---|---|
| 1 | 260804 | gpt-5.6-sol | `019fcc96-6587-7701-938c-335833f1f6bd` | 한도 소진 |
| 2 | 260805 | gpt-5.6-sol | `019fcfae-9078-7090-8740-b607d0eefd8d` | 한도 소진 |
| 3 | 260805 | gpt-5.6-terra | `019fcfc5-22e8-7df0-908d-9b29a09cb504` | 한도 소진 |

세 번 모두 **리셋 시각이 동일**했다 → 한도는 «모델 단위»가 아니라 **«계정 단위»**다.
심화문제집 값대조 건도 같은 한도를 기다리고 있으니 **리셋 후 두 건을 함께** 처리할 것.

## 이미 자체 검증으로 «해소»된 항목 — 다시 물을 필요 없음

차단 상태에서 기계적으로 판정 가능한 것만 골라 실측했다. 이건 독립 검토를
대신하지 않지만, 같은 걸 두 번 확인하느라 라운드를 쓰지는 말자.

- ✅ **§84 3호(과세최저한) ↔ §64의3②(250만원 공제) 이중 적용 없음**
  소득 200만 / 250만 / 250만+1원 / 300만 / 500만 전수 대조, 손계산과 전건 일치
  (예: 소득 300만 → 과표 50만 → 총 110,000원)
- ✅ **음수 소득 방어** — 손실>이익, 부대비용>수입 모두 과표 0·세액 0
- ✅ **전역 스코프 이름 충돌 0건** — `project/src/*.jsx` 최상위 식별자 전수 대조
- ✅ **showIf 로 숨겨진 stale 값이 계산에 안 섞임** (4경로)
  · 가상자산 `heldBefore='no'` + `marketAt2026` 잔존 → 취득가액·세액 동일
  · 양도세 다주택인데 `seniorMove='yes'` 잔존 → 고령감면 0
  · 종부세 1주택인데 `residentValue` 잔존 → 공제 동일
  · 종부세 합계 0원 → 0원 / 거주용>합계 → 비율 1로 클램프(4억+5억=9억)

## ★ 최우선으로 물어야 할 미검증 2건

둘 다 «판단»이 필요해 자체 검증으로 대체 불가하다. 라운드 1에서 반드시 답을 받을 것.

1. **양도세 중과세율에 누진공제를 그대로 차감하는 구조**
   `ReportReform2026.jsx` — `과표 × (기본세율 + 가산%p) − 누진공제`
   이 산식이 홈택스·실무 산식과 일치하는가? 중과 시 누진공제를 그대로 빼는 것이
   맞는지, 아니면 별도 처리가 필요한지.
2. **1세대1주택 판정을 «보유 2년»으로만 단순화한 것의 왜곡 범위**
   조정대상지역 취득분의 «거주 2년» 요건을 보지 않는다. 어떤 입력 조합에서
   결과가 실제와 갈리며, 그 오차가 이용자를 오도할 수준인가.

## 검토 대상 (배포 완료 상태 = main `23dc025`)

- `project/src/ReportCrypto2027.jsx` — 가상자산 소득세 (2027.1.1 시행 예정)
- `project/src/ReportReform2026.jsx` — 2026 세제개편안 양도세·종부세 연도별 비교
- `project/src/Report.jsx` — 라우팅·허브 등록
- `project/src/Report{Property,Comprehensive,Acquisition}.jsx` — jt-calc 이식분 + 공시가 경고
- `project/insights/build-insights.mjs` — 마크다운 표 렌더링 신규
- `project/tests_{cr_crypto2027,rf_reform2026}.js` — 검산 45건
- `calculators/{crypto-tax-2027,2026-tax-reform-*}.html` — SEO 랜딩
